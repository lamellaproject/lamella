// GENERATED by lamella-bsp-gen from parts/bmx280/bme280.toml + parts/bmx280/bmx280.toml -- DO NOT EDIT.
namespace Lamella.Generated
{
    public sealed class Bme280Part
    {
        private Bme280Part() { }
        public const string PART = "bme280";
        public const string FAMILY = "bmx280";

        /// identity: the accepted values are a SET. A driver that accepts only one of them rejects a genuine part, and a rejected part reads as no part at all -- so on a mismatch, name the id received AND this set.
        public const uint IDENTITY_REG = 0xD0;
        public const uint IDENTITY_WIDTH = 8;
        public const uint IDENTITY_VALUE_COUNT = 1;
        public const uint IDENTITY_VALUE_0 = 0x60;

        /// address: a base plus which pin contributes which bit. The part states the range; a carrier fixes the straps, so no strap is defaulted here.
        public const string ADDRESS_BUS = "i2c";
        public const uint ADDRESS_BASE = 0x76;
        public const uint ADDRESS_STRAP_COUNT = 1;
        public const uint ADDRESS_STRAP_SDO_BIT = 0;
        public const uint ADDRESS_STRAP_SDO_LOW = 0x76;
        public const uint ADDRESS_STRAP_SDO_HIGH = 0x77;

        /// buses: the register MAP is shared between a part's buses; the ADDRESS TRANSFORM is not, so each states its own as a named dispatch. A path that took the wrong one is wrong by a fixed bit, and on one direction only.
        public const uint BUS_COUNT = 2;
        public const string BUS_I2C_KIND = "i2c";
        public const string BUS_I2C_REGISTER_READ_TRANSFORM = "identity";
        public const string BUS_I2C_REGISTER_WRITE_TRANSFORM = "identity";
        public const string BUS_I2C_READ_PROTOCOL = "write-register-then-repeated-start-read";
        public const string BUS_SPI_KIND = "spi";
        public const string BUS_SPI_REGISTER_READ_TRANSFORM = "set-bit7";
        public const string BUS_SPI_REGISTER_WRITE_TRANSFORM = "clear-bit7";
        public const string BUS_SPI_READ_PROTOCOL = "single-chip-select-address-then-clock-data";
        public const uint BUS_SPI_MODE_COUNT = 2;
        public const uint BUS_SPI_MODE_0 = 0;
        public const uint BUS_SPI_MODE_1 = 3;

        /// registers: `_REG` is the address written on the wire -- an operand, NOT an offset to add to an instance base. Fields carry the shifted mask and its shift.
        public const uint ID_REG = 0xD0;
        public const uint ID_WIDTH = 8;
        public const string ID_ACCESS = "read-only";
        public const uint ID_CHIP_ID = 0xFF;
        public const uint ID_CHIP_ID_LSB = 0;
        public const uint RESET_REG = 0xE0;
        public const uint RESET_WIDTH = 8;
        public const string RESET_ACCESS = "write-only";
        public const uint RESET_RESET = 0xFF;
        public const uint RESET_RESET_LSB = 0;
        public const uint STATUS_REG = 0xF3;
        public const uint STATUS_WIDTH = 8;
        public const string STATUS_ACCESS = "read-only";
        public const uint STATUS_IM_UPDATE = 0x1;
        public const uint STATUS_IM_UPDATE_LSB = 0;
        public const uint STATUS_MEASURING = 0x8;
        public const uint STATUS_MEASURING_LSB = 3;
        public const uint CTRL_MEAS_REG = 0xF4;
        public const uint CTRL_MEAS_WIDTH = 8;
        public const string CTRL_MEAS_ACCESS = "read-write";
        public const uint CTRL_MEAS_MODE = 0x3;
        public const uint CTRL_MEAS_MODE_LSB = 0;
        public const uint CTRL_MEAS_OSRS_P = 0x1C;
        public const uint CTRL_MEAS_OSRS_P_LSB = 2;
        public const uint CTRL_MEAS_OSRS_T = 0xE0;
        public const uint CTRL_MEAS_OSRS_T_LSB = 5;
        public const uint CONFIG_REG = 0xF5;
        public const uint CONFIG_WIDTH = 8;
        public const string CONFIG_ACCESS = "read-write";
        public const uint CONFIG_SPI3W_EN = 0x1;
        public const uint CONFIG_SPI3W_EN_LSB = 0;
        public const uint CONFIG_FILTER = 0x1C;
        public const uint CONFIG_FILTER_LSB = 2;
        public const uint CONFIG_T_SB = 0xE0;
        public const uint CONFIG_T_SB_LSB = 5;
        public const uint PRESS_REG = 0xF7;
        public const uint PRESS_WIDTH = 24;
        public const string PRESS_ACCESS = "read-only";
        public const uint PRESS_PRESS_MSB = 0xFF0000;
        public const uint PRESS_PRESS_MSB_LSB = 16;
        public const uint PRESS_PRESS_LSB = 0xFF00;
        public const uint PRESS_PRESS_LSB_LSB = 8;
        public const uint PRESS_PRESS_XLSB = 0xF0;
        public const uint PRESS_PRESS_XLSB_LSB = 4;
        public const uint TEMP_REG = 0xFA;
        public const uint TEMP_WIDTH = 24;
        public const string TEMP_ACCESS = "read-only";
        public const uint TEMP_TEMP_MSB = 0xFF0000;
        public const uint TEMP_TEMP_MSB_LSB = 16;
        public const uint TEMP_TEMP_LSB = 0xFF00;
        public const uint TEMP_TEMP_LSB_LSB = 8;
        public const uint TEMP_TEMP_XLSB = 0xF0;
        public const uint TEMP_TEMP_XLSB_LSB = 4;
        public const uint CTRL_HUM_REG = 0xF2;
        public const uint CTRL_HUM_WIDTH = 8;
        public const string CTRL_HUM_ACCESS = "read-write";
        public const uint CTRL_HUM_OSRS_H = 0x7;
        public const uint CTRL_HUM_OSRS_H_LSB = 0;
        public const uint HUM_REG = 0xFD;
        public const uint HUM_WIDTH = 16;
        public const string HUM_ACCESS = "read-only";
        public const uint HUM_HUM_MSB = 0xFF00;
        public const uint HUM_HUM_MSB_LSB = 8;
        public const uint HUM_HUM_LSB = 0xFF;
        public const uint HUM_HUM_LSB_LSB = 0;

        /// encodings: the codes a field takes, as this part reads them.
        public const uint OVERSAMPLING_SKIPPED = 0;
        public const uint OVERSAMPLING_X1 = 1;
        public const uint OVERSAMPLING_X2 = 2;
        public const uint OVERSAMPLING_X4 = 3;
        public const uint OVERSAMPLING_X8 = 4;
        public const uint OVERSAMPLING_X16 = 5;
        public const uint MODE_SLEEP = 0;
        public const uint MODE_FORCED = 1;
        public const uint MODE_NORMAL = 3;
        public const uint FILTER_OFF = 0;
        public const uint FILTER_X2 = 1;
        public const uint FILTER_X4 = 2;
        public const uint FILTER_X8 = 3;
        public const uint FILTER_X16 = 4;
        public const uint RESET_COMMAND_SOFT_RESET = 0xB6;
        public const uint STANDBY_US_0 = 500;
        public const uint STANDBY_US_1 = 62500;
        public const uint STANDBY_US_2 = 125000;
        public const uint STANDBY_US_3 = 250000;
        public const uint STANDBY_US_4 = 500000;
        public const uint STANDBY_US_5 = 1000000;
        public const uint STANDBY_US_6 = 10000;
        public const uint STANDBY_US_7 = 20000;

        /// measurement: the part holds a whole data block steady for the duration of one burst, so a byte-at-a-time read can mix two conversions.
        public const uint BURST_LENGTH = 8;

        /// calibration 'temperature': these parameters are READ from the part and are per-device -- they are not constants and must not be baked into a driver. `_FORM` selects hand-written per-language arithmetic; signedness is not uniform, and reading it backwards yields a plausible wrong answer rather than an error.
        public const string TEMPERATURE_FORM = "bmx280-t-int32";
        public const string TEMPERATURE_BYTE_ORDER = "little";
        public const string TEMPERATURE_OUTPUT_SCALE = "centi-degrees-celsius";
        public const uint TEMPERATURE_READ_COUNT = 3;
        public const uint TEMPERATURE_DIG_T1_REG = 0x88;
        public const uint TEMPERATURE_DIG_T1_WIDTH = 16;
        public const uint TEMPERATURE_DIG_T1_SIGNED = 0;
        public const uint TEMPERATURE_DIG_T2_REG = 0x8A;
        public const uint TEMPERATURE_DIG_T2_WIDTH = 16;
        public const uint TEMPERATURE_DIG_T2_SIGNED = 1;
        public const uint TEMPERATURE_DIG_T3_REG = 0x8C;
        public const uint TEMPERATURE_DIG_T3_WIDTH = 16;
        public const uint TEMPERATURE_DIG_T3_SIGNED = 1;

        /// calibration 'pressure': these parameters are READ from the part and are per-device -- they are not constants and must not be baked into a driver. `_FORM` selects hand-written per-language arithmetic; signedness is not uniform, and reading it backwards yields a plausible wrong answer rather than an error.
        public const string PRESSURE_FORM = "bmx280-p-int64";
        public const string PRESSURE_BYTE_ORDER = "little";
        public const string PRESSURE_OUTPUT_SCALE = "q24.8-pascals";
        public const uint PRESSURE_DEPENDS_ON_COUNT = 1;
        public const string PRESSURE_DEPENDS_ON_0 = "temperature";
        public const uint PRESSURE_READ_COUNT = 9;
        public const uint PRESSURE_DIG_P1_REG = 0x8E;
        public const uint PRESSURE_DIG_P1_WIDTH = 16;
        public const uint PRESSURE_DIG_P1_SIGNED = 0;
        public const uint PRESSURE_DIG_P2_REG = 0x90;
        public const uint PRESSURE_DIG_P2_WIDTH = 16;
        public const uint PRESSURE_DIG_P2_SIGNED = 1;
        public const uint PRESSURE_DIG_P3_REG = 0x92;
        public const uint PRESSURE_DIG_P3_WIDTH = 16;
        public const uint PRESSURE_DIG_P3_SIGNED = 1;
        public const uint PRESSURE_DIG_P4_REG = 0x94;
        public const uint PRESSURE_DIG_P4_WIDTH = 16;
        public const uint PRESSURE_DIG_P4_SIGNED = 1;
        public const uint PRESSURE_DIG_P5_REG = 0x96;
        public const uint PRESSURE_DIG_P5_WIDTH = 16;
        public const uint PRESSURE_DIG_P5_SIGNED = 1;
        public const uint PRESSURE_DIG_P6_REG = 0x98;
        public const uint PRESSURE_DIG_P6_WIDTH = 16;
        public const uint PRESSURE_DIG_P6_SIGNED = 1;
        public const uint PRESSURE_DIG_P7_REG = 0x9A;
        public const uint PRESSURE_DIG_P7_WIDTH = 16;
        public const uint PRESSURE_DIG_P7_SIGNED = 1;
        public const uint PRESSURE_DIG_P8_REG = 0x9C;
        public const uint PRESSURE_DIG_P8_WIDTH = 16;
        public const uint PRESSURE_DIG_P8_SIGNED = 1;
        public const uint PRESSURE_DIG_P9_REG = 0x9E;
        public const uint PRESSURE_DIG_P9_WIDTH = 16;
        public const uint PRESSURE_DIG_P9_SIGNED = 1;

        /// calibration 'humidity': these parameters are READ from the part and are per-device -- they are not constants and must not be baked into a driver. `_FORM` selects hand-written per-language arithmetic; signedness is not uniform, and reading it backwards yields a plausible wrong answer rather than an error.
        public const string HUMIDITY_FORM = "bmx280-h-int32";
        public const string HUMIDITY_BYTE_ORDER = "little";
        public const string HUMIDITY_OUTPUT_SCALE = "q22.10-percent-relative-humidity";
        public const uint HUMIDITY_DEPENDS_ON_COUNT = 1;
        public const string HUMIDITY_DEPENDS_ON_0 = "temperature";
        public const uint HUMIDITY_READ_COUNT = 6;
        public const uint HUMIDITY_DIG_H1_REG = 0xA1;
        public const uint HUMIDITY_DIG_H1_WIDTH = 8;
        public const uint HUMIDITY_DIG_H1_SIGNED = 0;
        public const uint HUMIDITY_DIG_H2_REG = 0xE1;
        public const uint HUMIDITY_DIG_H2_WIDTH = 16;
        public const uint HUMIDITY_DIG_H2_SIGNED = 1;
        public const uint HUMIDITY_DIG_H3_REG = 0xE3;
        public const uint HUMIDITY_DIG_H3_WIDTH = 8;
        public const uint HUMIDITY_DIG_H3_SIGNED = 0;
        public const uint HUMIDITY_DIG_H4_REG = 0xE4;
        public const uint HUMIDITY_DIG_H4_WIDTH = 12;
        public const uint HUMIDITY_DIG_H4_SIGNED = 1;
        public const string HUMIDITY_DIG_H4_PACKING = "high8-at-0xE4-low4-in-low-nibble-of-0xE5";
        public const uint HUMIDITY_DIG_H5_REG = 0xE6;
        public const uint HUMIDITY_DIG_H5_WIDTH = 12;
        public const uint HUMIDITY_DIG_H5_SIGNED = 1;
        public const string HUMIDITY_DIG_H5_PACKING = "high8-at-0xE6-low4-in-high-nibble-of-0xE5";
        public const uint HUMIDITY_DIG_H6_REG = 0xE7;
        public const uint HUMIDITY_DIG_H6_WIDTH = 8;
        public const uint HUMIDITY_DIG_H6_SIGNED = 1;

        /// sequence 'forced_measurement': declarative steps, resolved here so a step carries its own register and mask. The description is transport agnostic -- one step list describes an I2C transaction, an SPI one, and a call through a host import alike.
        public const uint FORCED_MEASUREMENT_STEP_COUNT = 3;
        public const string FORCED_MEASUREMENT_STEP_0_OP = "write-field";
        public const string FORCED_MEASUREMENT_STEP_0_TARGET = "ctrl_meas.mode";
        public const uint FORCED_MEASUREMENT_STEP_0_REG = 0xF4;
        public const uint FORCED_MEASUREMENT_STEP_0_MASK = 0x3;
        public const uint FORCED_MEASUREMENT_STEP_0_LSB = 0;
        public const uint FORCED_MEASUREMENT_STEP_0_VALUE = 1;
        public const uint FORCED_MEASUREMENT_STEP_0_BOUNDED = 0;
        public const string FORCED_MEASUREMENT_STEP_1_OP = "poll-field-until";
        public const string FORCED_MEASUREMENT_STEP_1_TARGET = "status.measuring";
        public const uint FORCED_MEASUREMENT_STEP_1_REG = 0xF3;
        public const uint FORCED_MEASUREMENT_STEP_1_MASK = 0x8;
        public const uint FORCED_MEASUREMENT_STEP_1_LSB = 3;
        public const uint FORCED_MEASUREMENT_STEP_1_VALUE = 0;
        public const uint FORCED_MEASUREMENT_STEP_1_BOUNDED = 1;
        public const string FORCED_MEASUREMENT_STEP_2_OP = "burst-read";
        public const string FORCED_MEASUREMENT_STEP_2_TARGET = "press";
        public const uint FORCED_MEASUREMENT_STEP_2_REG = 0xF7;
        public const uint FORCED_MEASUREMENT_STEP_2_LENGTH = 8;
        public const uint FORCED_MEASUREMENT_STEP_2_BOUNDED = 0;
    }
}
