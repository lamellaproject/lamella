//! GENERATED by lamella-bsp-gen from parts/bmx280/bme280.toml + parts/bmx280/bmx280.toml -- DO NOT EDIT.
//! Regenerate: cargo run -p lamella-bsp-gen -- gen-parts . bmx280
pub const PART: &str = "bme280";
pub const FAMILY: &str = "bmx280";

/// sourcing: how far this part is established, on two INDEPENDENT axes -- a part can be strong on one and absent on the other, which is why they are not one rank. FACTS is `primary` (read from the part's own datasheet) or `secondary` (a primary vendor statement about this part carried by another document, named by DERIVED_FROM). VALIDATION is `none` (no physical part of this type has been made to answer), `identified` (one answered its identity register, against a negative control that tells the part from the wire) or `exercised` (one produced measurements a driver decoded). Anything above `none` states what was observed in EVIDENCE.
pub const SOURCING_FACTS: &str = "primary";
pub const SOURCING_VALIDATION: &str = "none";

/// identity: the accepted values are a SET. A driver that accepts only one of them rejects a genuine part, and a rejected part reads as no part at all -- so on a mismatch, name the id received AND this set.
pub const IDENTITY_REG: u32 = 0xD0;
pub const IDENTITY_WIDTH: u32 = 8;
pub const IDENTITY_VALUE_COUNT: u32 = 1;
pub const IDENTITY_VALUE_0: u32 = 0x60;

/// address: a base plus which pin contributes which bit. The part states the range; a carrier fixes the straps, so no strap is defaulted here.
pub const ADDRESS_BUS: &str = "i2c";
pub const ADDRESS_BASE: u32 = 0x76;
pub const ADDRESS_STRAP_COUNT: u32 = 1;
pub const ADDRESS_STRAP_SDO_BIT: u32 = 0;
pub const ADDRESS_STRAP_SDO_LOW: u32 = 0x76;
pub const ADDRESS_STRAP_SDO_HIGH: u32 = 0x77;

/// buses: the register MAP is shared between a part's buses; the ADDRESS TRANSFORM is not, so each states its own as a named dispatch. A path that took the wrong one is wrong by a fixed bit, and on one direction only.
pub const BUS_COUNT: u32 = 2;
pub const BUS_I2C_KIND: &str = "i2c";
pub const BUS_I2C_REGISTER_READ_TRANSFORM: &str = "identity";
pub const BUS_I2C_REGISTER_WRITE_TRANSFORM: &str = "identity";
pub const BUS_I2C_READ_PROTOCOL: &str = "write-register-then-repeated-start-read";
pub const BUS_SPI_KIND: &str = "spi";
pub const BUS_SPI_REGISTER_READ_TRANSFORM: &str = "set-bit7";
pub const BUS_SPI_REGISTER_WRITE_TRANSFORM: &str = "clear-bit7";
pub const BUS_SPI_READ_PROTOCOL: &str = "single-chip-select-address-then-clock-data";
pub const BUS_SPI_MODE_COUNT: u32 = 2;
pub const BUS_SPI_MODE_0: u32 = 0;
pub const BUS_SPI_MODE_1: u32 = 3;

/// registers: `_REG` is the address written on the wire -- an operand, NOT an offset to add to an instance base. Fields carry the shifted mask and its shift.
pub const ID_REG: u32 = 0xD0;
pub const ID_WIDTH: u32 = 8;
pub const ID_ACCESS: &str = "read-only";
pub const ID_CHIP_ID: u32 = 0xFF;
pub const ID_CHIP_ID_LSB: u32 = 0;
pub const RESET_REG: u32 = 0xE0;
pub const RESET_WIDTH: u32 = 8;
pub const RESET_ACCESS: &str = "write-only";
pub const RESET_RESET: u32 = 0xFF;
pub const RESET_RESET_LSB: u32 = 0;
pub const STATUS_REG: u32 = 0xF3;
pub const STATUS_WIDTH: u32 = 8;
pub const STATUS_ACCESS: &str = "read-only";
pub const STATUS_IM_UPDATE: u32 = 0x1;
pub const STATUS_IM_UPDATE_LSB: u32 = 0;
pub const STATUS_MEASURING: u32 = 0x8;
pub const STATUS_MEASURING_LSB: u32 = 3;
pub const CTRL_MEAS_REG: u32 = 0xF4;
pub const CTRL_MEAS_WIDTH: u32 = 8;
pub const CTRL_MEAS_ACCESS: &str = "read-write";
pub const CTRL_MEAS_MODE: u32 = 0x3;
pub const CTRL_MEAS_MODE_LSB: u32 = 0;
pub const CTRL_MEAS_OSRS_P: u32 = 0x1C;
pub const CTRL_MEAS_OSRS_P_LSB: u32 = 2;
pub const CTRL_MEAS_OSRS_T: u32 = 0xE0;
pub const CTRL_MEAS_OSRS_T_LSB: u32 = 5;
pub const CONFIG_REG: u32 = 0xF5;
pub const CONFIG_WIDTH: u32 = 8;
pub const CONFIG_ACCESS: &str = "read-write";
pub const CONFIG_SPI3W_EN: u32 = 0x1;
pub const CONFIG_SPI3W_EN_LSB: u32 = 0;
pub const CONFIG_FILTER: u32 = 0x1C;
pub const CONFIG_FILTER_LSB: u32 = 2;
pub const CONFIG_T_SB: u32 = 0xE0;
pub const CONFIG_T_SB_LSB: u32 = 5;
pub const PRESS_REG: u32 = 0xF7;
pub const PRESS_WIDTH: u32 = 24;
pub const PRESS_ACCESS: &str = "read-only";
pub const PRESS_PRESS_MSB: u32 = 0xFF0000;
pub const PRESS_PRESS_MSB_LSB: u32 = 16;
pub const PRESS_PRESS_LSB: u32 = 0xFF00;
pub const PRESS_PRESS_LSB_LSB: u32 = 8;
pub const PRESS_PRESS_XLSB: u32 = 0xF0;
pub const PRESS_PRESS_XLSB_LSB: u32 = 4;
pub const TEMP_REG: u32 = 0xFA;
pub const TEMP_WIDTH: u32 = 24;
pub const TEMP_ACCESS: &str = "read-only";
pub const TEMP_TEMP_MSB: u32 = 0xFF0000;
pub const TEMP_TEMP_MSB_LSB: u32 = 16;
pub const TEMP_TEMP_LSB: u32 = 0xFF00;
pub const TEMP_TEMP_LSB_LSB: u32 = 8;
pub const TEMP_TEMP_XLSB: u32 = 0xF0;
pub const TEMP_TEMP_XLSB_LSB: u32 = 4;
pub const CTRL_HUM_REG: u32 = 0xF2;
pub const CTRL_HUM_WIDTH: u32 = 8;
pub const CTRL_HUM_ACCESS: &str = "read-write";
pub const CTRL_HUM_OSRS_H: u32 = 0x7;
pub const CTRL_HUM_OSRS_H_LSB: u32 = 0;
pub const HUM_REG: u32 = 0xFD;
pub const HUM_WIDTH: u32 = 16;
pub const HUM_ACCESS: &str = "read-only";
pub const HUM_HUM_MSB: u32 = 0xFF00;
pub const HUM_HUM_MSB_LSB: u32 = 8;
pub const HUM_HUM_LSB: u32 = 0xFF;
pub const HUM_HUM_LSB_LSB: u32 = 0;

/// encodings: the codes a field takes, as this part reads them.
pub const OVERSAMPLING_SKIPPED: u32 = 0;
pub const OVERSAMPLING_X1: u32 = 1;
pub const OVERSAMPLING_X2: u32 = 2;
pub const OVERSAMPLING_X4: u32 = 3;
pub const OVERSAMPLING_X8: u32 = 4;
pub const OVERSAMPLING_X16: u32 = 5;
pub const MODE_SLEEP: u32 = 0;
pub const MODE_FORCED: u32 = 1;
pub const MODE_NORMAL: u32 = 3;
pub const FILTER_OFF: u32 = 0;
pub const FILTER_X2: u32 = 1;
pub const FILTER_X4: u32 = 2;
pub const FILTER_X8: u32 = 3;
pub const FILTER_X16: u32 = 4;
pub const RESET_COMMAND_SOFT_RESET: u32 = 0xB6;
pub const STANDBY_US_0: u32 = 500;
pub const STANDBY_US_1: u32 = 62500;
pub const STANDBY_US_2: u32 = 125000;
pub const STANDBY_US_3: u32 = 250000;
pub const STANDBY_US_4: u32 = 500000;
pub const STANDBY_US_5: u32 = 1000000;
pub const STANDBY_US_6: u32 = 10000;
pub const STANDBY_US_7: u32 = 20000;

/// measurement: the part holds a whole data block steady for the duration of one burst, so a byte-at-a-time read can mix two conversions.
pub const BURST_LENGTH: u32 = 8;

/// calibration 'temperature': these parameters are READ from the part and are per-device -- they are not constants and must not be baked into a driver. `_FORM` selects hand-written per-language arithmetic; signedness is not uniform, and reading it backwards yields a plausible wrong answer rather than an error.
pub const TEMPERATURE_FORM: &str = "bmx280-t-int32";
pub const TEMPERATURE_BYTE_ORDER: &str = "little";
pub const TEMPERATURE_OUTPUT_SCALE: &str = "centi-degrees-celsius";
pub const TEMPERATURE_READ_COUNT: u32 = 3;
pub const TEMPERATURE_DIG_T1_REG: u32 = 0x88;
pub const TEMPERATURE_DIG_T1_WIDTH: u32 = 16;
pub const TEMPERATURE_DIG_T1_SIGNED: u32 = 0;
pub const TEMPERATURE_DIG_T2_REG: u32 = 0x8A;
pub const TEMPERATURE_DIG_T2_WIDTH: u32 = 16;
pub const TEMPERATURE_DIG_T2_SIGNED: u32 = 1;
pub const TEMPERATURE_DIG_T3_REG: u32 = 0x8C;
pub const TEMPERATURE_DIG_T3_WIDTH: u32 = 16;
pub const TEMPERATURE_DIG_T3_SIGNED: u32 = 1;

/// calibration 'pressure': these parameters are READ from the part and are per-device -- they are not constants and must not be baked into a driver. `_FORM` selects hand-written per-language arithmetic; signedness is not uniform, and reading it backwards yields a plausible wrong answer rather than an error.
pub const PRESSURE_FORM: &str = "bmx280-p-int64";
pub const PRESSURE_BYTE_ORDER: &str = "little";
pub const PRESSURE_OUTPUT_SCALE: &str = "q24.8-pascals";
pub const PRESSURE_DEPENDS_ON_COUNT: u32 = 1;
pub const PRESSURE_DEPENDS_ON_0: &str = "temperature";
pub const PRESSURE_READ_COUNT: u32 = 9;
pub const PRESSURE_DIG_P1_REG: u32 = 0x8E;
pub const PRESSURE_DIG_P1_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P1_SIGNED: u32 = 0;
pub const PRESSURE_DIG_P2_REG: u32 = 0x90;
pub const PRESSURE_DIG_P2_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P2_SIGNED: u32 = 1;
pub const PRESSURE_DIG_P3_REG: u32 = 0x92;
pub const PRESSURE_DIG_P3_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P3_SIGNED: u32 = 1;
pub const PRESSURE_DIG_P4_REG: u32 = 0x94;
pub const PRESSURE_DIG_P4_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P4_SIGNED: u32 = 1;
pub const PRESSURE_DIG_P5_REG: u32 = 0x96;
pub const PRESSURE_DIG_P5_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P5_SIGNED: u32 = 1;
pub const PRESSURE_DIG_P6_REG: u32 = 0x98;
pub const PRESSURE_DIG_P6_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P6_SIGNED: u32 = 1;
pub const PRESSURE_DIG_P7_REG: u32 = 0x9A;
pub const PRESSURE_DIG_P7_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P7_SIGNED: u32 = 1;
pub const PRESSURE_DIG_P8_REG: u32 = 0x9C;
pub const PRESSURE_DIG_P8_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P8_SIGNED: u32 = 1;
pub const PRESSURE_DIG_P9_REG: u32 = 0x9E;
pub const PRESSURE_DIG_P9_WIDTH: u32 = 16;
pub const PRESSURE_DIG_P9_SIGNED: u32 = 1;

/// calibration 'humidity': these parameters are READ from the part and are per-device -- they are not constants and must not be baked into a driver. `_FORM` selects hand-written per-language arithmetic; signedness is not uniform, and reading it backwards yields a plausible wrong answer rather than an error.
pub const HUMIDITY_FORM: &str = "bmx280-h-int32";
pub const HUMIDITY_BYTE_ORDER: &str = "little";
pub const HUMIDITY_OUTPUT_SCALE: &str = "q22.10-percent-relative-humidity";
pub const HUMIDITY_DEPENDS_ON_COUNT: u32 = 1;
pub const HUMIDITY_DEPENDS_ON_0: &str = "temperature";
pub const HUMIDITY_READ_COUNT: u32 = 6;
pub const HUMIDITY_DIG_H1_REG: u32 = 0xA1;
pub const HUMIDITY_DIG_H1_WIDTH: u32 = 8;
pub const HUMIDITY_DIG_H1_SIGNED: u32 = 0;
pub const HUMIDITY_DIG_H2_REG: u32 = 0xE1;
pub const HUMIDITY_DIG_H2_WIDTH: u32 = 16;
pub const HUMIDITY_DIG_H2_SIGNED: u32 = 1;
pub const HUMIDITY_DIG_H3_REG: u32 = 0xE3;
pub const HUMIDITY_DIG_H3_WIDTH: u32 = 8;
pub const HUMIDITY_DIG_H3_SIGNED: u32 = 0;
pub const HUMIDITY_DIG_H4_REG: u32 = 0xE4;
pub const HUMIDITY_DIG_H4_WIDTH: u32 = 12;
pub const HUMIDITY_DIG_H4_SIGNED: u32 = 1;
pub const HUMIDITY_DIG_H4_PACKING: &str = "high8-at-0xE4-low4-in-low-nibble-of-0xE5";
pub const HUMIDITY_DIG_H5_REG: u32 = 0xE6;
pub const HUMIDITY_DIG_H5_WIDTH: u32 = 12;
pub const HUMIDITY_DIG_H5_SIGNED: u32 = 1;
pub const HUMIDITY_DIG_H5_PACKING: &str = "high8-at-0xE6-low4-in-high-nibble-of-0xE5";
pub const HUMIDITY_DIG_H6_REG: u32 = 0xE7;
pub const HUMIDITY_DIG_H6_WIDTH: u32 = 8;
pub const HUMIDITY_DIG_H6_SIGNED: u32 = 1;

/// sequence 'forced_measurement': declarative steps, resolved here so a step carries its own register and mask. The description is transport agnostic -- one step list describes an I2C transaction, an SPI one, and a call through a host import alike.
pub const FORCED_MEASUREMENT_STEP_COUNT: u32 = 3;
pub const FORCED_MEASUREMENT_STEP_0_OP: &str = "write-field";
pub const FORCED_MEASUREMENT_STEP_0_TARGET: &str = "ctrl_meas.mode";
pub const FORCED_MEASUREMENT_STEP_0_REG: u32 = 0xF4;
pub const FORCED_MEASUREMENT_STEP_0_MASK: u32 = 0x3;
pub const FORCED_MEASUREMENT_STEP_0_LSB: u32 = 0;
pub const FORCED_MEASUREMENT_STEP_0_VALUE: u32 = 1;
pub const FORCED_MEASUREMENT_STEP_0_BOUNDED: u32 = 0;
pub const FORCED_MEASUREMENT_STEP_1_OP: &str = "poll-field-until";
pub const FORCED_MEASUREMENT_STEP_1_TARGET: &str = "status.measuring";
pub const FORCED_MEASUREMENT_STEP_1_REG: u32 = 0xF3;
pub const FORCED_MEASUREMENT_STEP_1_MASK: u32 = 0x8;
pub const FORCED_MEASUREMENT_STEP_1_LSB: u32 = 3;
pub const FORCED_MEASUREMENT_STEP_1_VALUE: u32 = 0;
pub const FORCED_MEASUREMENT_STEP_1_BOUNDED: u32 = 1;
pub const FORCED_MEASUREMENT_STEP_2_OP: &str = "burst-read";
pub const FORCED_MEASUREMENT_STEP_2_TARGET: &str = "press";
pub const FORCED_MEASUREMENT_STEP_2_REG: u32 = 0xF7;
pub const FORCED_MEASUREMENT_STEP_2_LENGTH: u32 = 8;
pub const FORCED_MEASUREMENT_STEP_2_BOUNDED: u32 = 0;
