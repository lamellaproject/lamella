//! GENERATED by lamella-bsp-gen from parts/lsm303agr/lsm303agr-xl.toml + parts/lsm303agr/lsm303agr.toml -- DO NOT EDIT.
//! Regenerate: cargo run -p lamella-bsp-gen -- gen-parts . lsm303agr
pub const PART: &str = "lsm303agr-xl";
pub const FAMILY: &str = "lsm303agr";

/// sourcing: how far this part is established, on two INDEPENDENT axes -- a part can be strong on one and absent on the other, which is why they are not one rank. FACTS is `primary` (read from the part's own datasheet) or `secondary` (a primary vendor statement about this part carried by another document, named by DERIVED_FROM). VALIDATION is `none` (no physical part of this type has been made to answer), `identified` (one answered its identity register, against a negative control that tells the part from the wire) or `exercised` (one produced measurements a driver decoded). Anything above `none` states what was observed in EVIDENCE.
pub const SOURCING_FACTS: &str = "primary";
pub const SOURCING_VALIDATION: &str = "identified";
pub const SOURCING_EVIDENCE: &str = "A physical part answered the identity register on a Raspberry Pi Pico 2, over I2C0 at 100 kHz, on a breakout reached through a four-wire cable: register 0x0F read 0x33 at address 0x19, the single value this table accepts at the single address it states, repeated across five consecutive passes with 0 faulted of 112 addresses walked in each. THE NEGATIVE CONTROL RAN AT THE SAME ADDRESS ON THE SAME REGISTER: with the module unplugged, the identical read ABORTED in four consecutive passes, during which nothing answered anywhere on the bus and 0 of 112 addresses were faulted -- so the byte came from a part rather than from the state of an idle wire. A SECOND CONTROL CAME FREE AND IT TESTS THIS FAMILY'S WHOLE MODEL: the other half of the same package was checked in the same passes at ITS address and ITS register, 0x4F at 0x1E, and answered its own value and not this one. Two addresses, two registers, two values, one package -- so the claim that this package is two bus residents is measured rather than argued. THE READING ALSO SETTLED WHICH VARIANT THE BREAKOUT CARRIES: an earlier walk of this same module recorded only that something answered at 0x19 and 0x1E, which narrowed it to an LSM303-class part and no further. EVERY NUMBER IN THE CHECK -- the address, the register and the accepted value -- was taken from this table's own emitted artifact by a bench program that includes it, so what was tested is the row that ships and not a copy of it.";

/// identity: the accepted values are a SET. A driver that accepts only one of them rejects a genuine part, and a rejected part reads as no part at all -- so on a mismatch, name the id received AND this set.
pub const IDENTITY_REG: u32 = 0xF;
pub const IDENTITY_WIDTH: u32 = 8;
pub const IDENTITY_VALUE_COUNT: u32 = 1;
pub const IDENTITY_VALUE_0: u32 = 0x33;

/// address: a base plus which pin contributes which bit. The part states the range; a carrier fixes the straps, so no strap is defaulted here.
pub const ADDRESS_BUS: &str = "i2c";
pub const ADDRESS_BASE: u32 = 0x19;
pub const ADDRESS_STRAP_COUNT: u32 = 0;

/// buses: the register MAP is shared between a part's buses; the ADDRESS TRANSFORM is not, so each states its own as a named dispatch. A path that took the wrong one is wrong by a fixed bit, and on one direction only.
pub const BUS_COUNT: u32 = 2;
pub const BUS_I2C_KIND: &str = "i2c";
pub const BUS_I2C_REGISTER_READ_TRANSFORM: &str = "identity";
pub const BUS_I2C_REGISTER_WRITE_TRANSFORM: &str = "identity";
pub const BUS_I2C_READ_PROTOCOL: &str = "write-register-then-repeated-start-read";
pub const BUS_SPI_KIND: &str = "spi";
pub const BUS_SPI_REGISTER_READ_TRANSFORM: &str = "set-bit7";
pub const BUS_SPI_REGISTER_WRITE_TRANSFORM: &str = "clear-bit7";
pub const BUS_SPI_READ_PROTOCOL: &str = "single-chip-select-address-then-clock-data";

/// registers: `_REG` is the address written on the wire -- an operand, NOT an offset to add to an instance base. Fields carry the shifted mask and its shift.
pub const WHO_AM_I_A_REG: u32 = 0xF;
pub const WHO_AM_I_A_WIDTH: u32 = 8;
pub const WHO_AM_I_A_ACCESS: &str = "read-only";
pub const WHO_AM_I_A_WHO_AM_I: u32 = 0xFF;
pub const WHO_AM_I_A_WHO_AM_I_LSB: u32 = 0;
pub const STATUS_REG_AUX_A_REG: u32 = 0x7;
pub const STATUS_REG_AUX_A_WIDTH: u32 = 8;
pub const STATUS_REG_AUX_A_ACCESS: &str = "read-only";
pub const OUT_TEMP_A_REG: u32 = 0xC;
pub const OUT_TEMP_A_WIDTH: u32 = 16;
pub const OUT_TEMP_A_ACCESS: &str = "read-only";
pub const OUT_TEMP_A_OUT_TEMP_L: u32 = 0xFF;
pub const OUT_TEMP_A_OUT_TEMP_L_LSB: u32 = 0;
pub const OUT_TEMP_A_OUT_TEMP_H: u32 = 0xFF00;
pub const OUT_TEMP_A_OUT_TEMP_H_LSB: u32 = 8;
pub const TEMP_CFG_REG_A_REG: u32 = 0x1F;
pub const TEMP_CFG_REG_A_WIDTH: u32 = 8;
pub const TEMP_CFG_REG_A_ACCESS: &str = "read-write";
pub const CTRL_REG1_A_REG: u32 = 0x20;
pub const CTRL_REG1_A_WIDTH: u32 = 8;
pub const CTRL_REG1_A_ACCESS: &str = "read-write";
pub const CTRL_REG2_A_REG: u32 = 0x21;
pub const CTRL_REG2_A_WIDTH: u32 = 8;
pub const CTRL_REG2_A_ACCESS: &str = "read-write";
pub const CTRL_REG3_A_REG: u32 = 0x22;
pub const CTRL_REG3_A_WIDTH: u32 = 8;
pub const CTRL_REG3_A_ACCESS: &str = "read-write";
pub const CTRL_REG4_A_REG: u32 = 0x23;
pub const CTRL_REG4_A_WIDTH: u32 = 8;
pub const CTRL_REG4_A_ACCESS: &str = "read-write";
pub const CTRL_REG5_A_REG: u32 = 0x24;
pub const CTRL_REG5_A_WIDTH: u32 = 8;
pub const CTRL_REG5_A_ACCESS: &str = "read-write";
pub const CTRL_REG6_A_REG: u32 = 0x25;
pub const CTRL_REG6_A_WIDTH: u32 = 8;
pub const CTRL_REG6_A_ACCESS: &str = "read-write";
pub const STATUS_REG_A_REG: u32 = 0x27;
pub const STATUS_REG_A_WIDTH: u32 = 8;
pub const STATUS_REG_A_ACCESS: &str = "read-only";
pub const OUT_X_A_REG: u32 = 0x28;
pub const OUT_X_A_WIDTH: u32 = 16;
pub const OUT_X_A_ACCESS: &str = "read-only";
pub const OUT_X_A_OUT_X_L: u32 = 0xFF;
pub const OUT_X_A_OUT_X_L_LSB: u32 = 0;
pub const OUT_X_A_OUT_X_H: u32 = 0xFF00;
pub const OUT_X_A_OUT_X_H_LSB: u32 = 8;
pub const OUT_Y_A_REG: u32 = 0x2A;
pub const OUT_Y_A_WIDTH: u32 = 16;
pub const OUT_Y_A_ACCESS: &str = "read-only";
pub const OUT_Y_A_OUT_Y_L: u32 = 0xFF;
pub const OUT_Y_A_OUT_Y_L_LSB: u32 = 0;
pub const OUT_Y_A_OUT_Y_H: u32 = 0xFF00;
pub const OUT_Y_A_OUT_Y_H_LSB: u32 = 8;
pub const OUT_Z_A_REG: u32 = 0x2C;
pub const OUT_Z_A_WIDTH: u32 = 16;
pub const OUT_Z_A_ACCESS: &str = "read-only";
pub const OUT_Z_A_OUT_Z_L: u32 = 0xFF;
pub const OUT_Z_A_OUT_Z_L_LSB: u32 = 0;
pub const OUT_Z_A_OUT_Z_H: u32 = 0xFF00;
pub const OUT_Z_A_OUT_Z_H_LSB: u32 = 8;
pub const FIFO_CTRL_REG_A_REG: u32 = 0x2E;
pub const FIFO_CTRL_REG_A_WIDTH: u32 = 8;
pub const FIFO_CTRL_REG_A_ACCESS: &str = "read-write";
pub const FIFO_SRC_REG_A_REG: u32 = 0x2F;
pub const FIFO_SRC_REG_A_WIDTH: u32 = 8;
pub const FIFO_SRC_REG_A_ACCESS: &str = "read-only";

/// measurement: the part holds a whole data block steady for the duration of one burst, so a byte-at-a-time read can mix two conversions.
pub const BURST_LENGTH: u32 = 6;
