// GENERATED by lamella-bsp-gen from parts/lsm303agr/lsm303agr-xl.toml + parts/lsm303agr/lsm303agr.toml -- DO NOT EDIT.
namespace Lamella.Generated
{
    public sealed class Lsm303agrXlPart
    {
        private Lsm303agrXlPart() { }
        public const string PART = "lsm303agr-xl";
        public const string FAMILY = "lsm303agr";

        /// sourcing: how far this part is established, on two INDEPENDENT axes -- a part can be strong on one and absent on the other, which is why they are not one rank. FACTS is `primary` (read from the part's own datasheet) or `secondary` (a primary vendor statement about this part carried by another document, named by DERIVED_FROM). VALIDATION is `none` (no physical part of this type has been made to answer), `identified` (one answered its identity register, against a negative control that tells the part from the wire) or `exercised` (one produced measurements a driver decoded). Anything above `none` states what was observed in EVIDENCE.
        public const string SOURCING_FACTS = "primary";
        public const string SOURCING_VALIDATION = "identified";
        public const string SOURCING_EVIDENCE = "A physical part answered the identity register on a Raspberry Pi Pico 2, over I2C0 at 100 kHz, on a breakout reached through a four-wire cable: register 0x0F read 0x33 at address 0x19, the single value this table accepts at the single address it states, repeated across five consecutive passes with 0 faulted of 112 addresses walked in each. THE NEGATIVE CONTROL RAN AT THE SAME ADDRESS ON THE SAME REGISTER: with the module unplugged, the identical read ABORTED in four consecutive passes, during which nothing answered anywhere on the bus and 0 of 112 addresses were faulted -- so the byte came from a part rather than from the state of an idle wire. A SECOND CONTROL CAME FREE AND IT TESTS THIS FAMILY'S WHOLE MODEL: the other half of the same package was checked in the same passes at ITS address and ITS register, 0x4F at 0x1E, and answered its own value and not this one. Two addresses, two registers, two values, one package -- so the claim that this package is two bus residents is measured rather than argued. THE READING ALSO SETTLED WHICH VARIANT THE BREAKOUT CARRIES: an earlier walk of this same module recorded only that something answered at 0x19 and 0x1E, which narrowed it to an LSM303-class part and no further. EVERY NUMBER IN THE CHECK -- the address, the register and the accepted value -- was taken from this table's own emitted artifact by a bench program that includes it, so what was tested is the row that ships and not a copy of it.";

        /// identity: the accepted values are a SET. A driver that accepts only one of them rejects a genuine part, and a rejected part reads as no part at all -- so on a mismatch, name the id received AND this set.
        public const uint IDENTITY_REG = 0xF;
        public const uint IDENTITY_WIDTH = 8;
        public const uint IDENTITY_VALUE_COUNT = 1;
        public const uint IDENTITY_VALUE_0 = 0x33;

        /// address: a base plus which pin contributes which bit. The part states the range; a carrier fixes the straps, so no strap is defaulted here.
        public const string ADDRESS_BUS = "i2c";
        public const uint ADDRESS_BASE = 0x19;
        public const uint ADDRESS_STRAP_COUNT = 0;

        /// buses: the register MAP is shared between a part's buses; the ADDRESS TRANSFORM is not, so each states its own as a named dispatch. A path that took the wrong one is wrong by a fixed bit, and on one direction only.
        public const uint BUS_COUNT = 2;
        public const string BUS_I2C_KIND = "i2c";
        public const string BUS_I2C_REGISTER_READ_TRANSFORM = "identity";
        public const string BUS_I2C_REGISTER_WRITE_TRANSFORM = "identity";
        public const string BUS_I2C_READ_PROTOCOL = "write-register-then-repeated-start-read";
        public const string BUS_SPI_KIND = "spi";
        public const string BUS_SPI_REGISTER_READ_TRANSFORM = "set-bit7";
        public const string BUS_SPI_REGISTER_WRITE_TRANSFORM = "clear-bit7";
        public const string BUS_SPI_READ_PROTOCOL = "single-chip-select-address-then-clock-data";

        /// registers: `_REG` is the address written on the wire -- an operand, NOT an offset to add to an instance base. Fields carry the shifted mask and its shift.
        public const uint WHO_AM_I_A_REG = 0xF;
        public const uint WHO_AM_I_A_WIDTH = 8;
        public const string WHO_AM_I_A_ACCESS = "read-only";
        public const uint WHO_AM_I_A_WHO_AM_I = 0xFF;
        public const uint WHO_AM_I_A_WHO_AM_I_LSB = 0;
        public const uint STATUS_REG_AUX_A_REG = 0x7;
        public const uint STATUS_REG_AUX_A_WIDTH = 8;
        public const string STATUS_REG_AUX_A_ACCESS = "read-only";
        public const uint OUT_TEMP_A_REG = 0xC;
        public const uint OUT_TEMP_A_WIDTH = 16;
        public const string OUT_TEMP_A_ACCESS = "read-only";
        public const uint OUT_TEMP_A_OUT_TEMP_L = 0xFF;
        public const uint OUT_TEMP_A_OUT_TEMP_L_LSB = 0;
        public const uint OUT_TEMP_A_OUT_TEMP_H = 0xFF00;
        public const uint OUT_TEMP_A_OUT_TEMP_H_LSB = 8;
        public const uint TEMP_CFG_REG_A_REG = 0x1F;
        public const uint TEMP_CFG_REG_A_WIDTH = 8;
        public const string TEMP_CFG_REG_A_ACCESS = "read-write";
        public const uint CTRL_REG1_A_REG = 0x20;
        public const uint CTRL_REG1_A_WIDTH = 8;
        public const string CTRL_REG1_A_ACCESS = "read-write";
        public const uint CTRL_REG2_A_REG = 0x21;
        public const uint CTRL_REG2_A_WIDTH = 8;
        public const string CTRL_REG2_A_ACCESS = "read-write";
        public const uint CTRL_REG3_A_REG = 0x22;
        public const uint CTRL_REG3_A_WIDTH = 8;
        public const string CTRL_REG3_A_ACCESS = "read-write";
        public const uint CTRL_REG4_A_REG = 0x23;
        public const uint CTRL_REG4_A_WIDTH = 8;
        public const string CTRL_REG4_A_ACCESS = "read-write";
        public const uint CTRL_REG5_A_REG = 0x24;
        public const uint CTRL_REG5_A_WIDTH = 8;
        public const string CTRL_REG5_A_ACCESS = "read-write";
        public const uint CTRL_REG6_A_REG = 0x25;
        public const uint CTRL_REG6_A_WIDTH = 8;
        public const string CTRL_REG6_A_ACCESS = "read-write";
        public const uint STATUS_REG_A_REG = 0x27;
        public const uint STATUS_REG_A_WIDTH = 8;
        public const string STATUS_REG_A_ACCESS = "read-only";
        public const uint OUT_X_A_REG = 0x28;
        public const uint OUT_X_A_WIDTH = 16;
        public const string OUT_X_A_ACCESS = "read-only";
        public const uint OUT_X_A_OUT_X_L = 0xFF;
        public const uint OUT_X_A_OUT_X_L_LSB = 0;
        public const uint OUT_X_A_OUT_X_H = 0xFF00;
        public const uint OUT_X_A_OUT_X_H_LSB = 8;
        public const uint OUT_Y_A_REG = 0x2A;
        public const uint OUT_Y_A_WIDTH = 16;
        public const string OUT_Y_A_ACCESS = "read-only";
        public const uint OUT_Y_A_OUT_Y_L = 0xFF;
        public const uint OUT_Y_A_OUT_Y_L_LSB = 0;
        public const uint OUT_Y_A_OUT_Y_H = 0xFF00;
        public const uint OUT_Y_A_OUT_Y_H_LSB = 8;
        public const uint OUT_Z_A_REG = 0x2C;
        public const uint OUT_Z_A_WIDTH = 16;
        public const string OUT_Z_A_ACCESS = "read-only";
        public const uint OUT_Z_A_OUT_Z_L = 0xFF;
        public const uint OUT_Z_A_OUT_Z_L_LSB = 0;
        public const uint OUT_Z_A_OUT_Z_H = 0xFF00;
        public const uint OUT_Z_A_OUT_Z_H_LSB = 8;
        public const uint FIFO_CTRL_REG_A_REG = 0x2E;
        public const uint FIFO_CTRL_REG_A_WIDTH = 8;
        public const string FIFO_CTRL_REG_A_ACCESS = "read-write";
        public const uint FIFO_SRC_REG_A_REG = 0x2F;
        public const uint FIFO_SRC_REG_A_WIDTH = 8;
        public const string FIFO_SRC_REG_A_ACCESS = "read-only";

        /// measurement: the part holds a whole data block steady for the duration of one burst, so a byte-at-a-time read can mix two conversions.
        public const uint BURST_LENGTH = 6;
    }
}
