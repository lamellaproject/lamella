// GENERATED by lamella-bsp-gen from parts/lsm303agr/lsm303agr-mag.toml + parts/lsm303agr/lsm303agr.toml -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-parts . lsm303agr
//
// The lsm303agr-mag part table, FLATTENED: every value it inherited is spelled out here, because a
// value that is only ever inherited is pinned in no emitted artifact and so cannot be
// checked for drift between languages. A part is something we talk TO rather than a chip
// we run on -- a board states which buses exist, this table states the part's identity,
// and a probe joins the two at run time.

public enum Lsm303agrMagPart {
    public static let PART: StaticString = "lsm303agr-mag"
    public static let FAMILY: StaticString = "lsm303agr"

    // sourcing: how far this part is established, on two INDEPENDENT axes -- a part can be strong on one and absent on the other, which is why they are not one rank. FACTS is `primary` (read from the part's own datasheet) or `secondary` (a primary vendor statement about this part carried by another document, named by DERIVED_FROM). VALIDATION is `none` (no physical part of this type has been made to answer), `identified` (one answered its identity register, against a negative control that tells the part from the wire) or `exercised` (one produced measurements a driver decoded). Anything above `none` states what was observed in EVIDENCE.
    public static let SOURCING_FACTS: StaticString = "primary"
    public static let SOURCING_VALIDATION: StaticString = "identified"
    public static let SOURCING_EVIDENCE: StaticString = "A physical part answered the identity register on a Raspberry Pi Pico 2, over I2C0 at 100 kHz, on a breakout reached through a four-wire cable: register 0x4F read 0x40 at address 0x1E, the single value this table accepts at the single address it states, repeated across five consecutive passes with 0 faulted of 112 addresses walked in each. THE NEGATIVE CONTROL RAN AT THE SAME ADDRESS ON THE SAME REGISTER: with the module unplugged, the identical read ABORTED in four consecutive passes, during which nothing answered anywhere on the bus and 0 of 112 addresses were faulted. A SECOND CONTROL CAME FREE AND IT TESTS THIS FAMILY'S WHOLE MODEL: the accelerometer half of the same package was checked in the same passes at ITS address and ITS register, 0x0F at 0x19, and answered its own value and not this one -- so the two halves are separated by different registers at different addresses on silicon and not merely on paper, which is the claim that made this package two parts rather than one with a plural address. EVERY NUMBER IN THE CHECK -- the address, the register and the accepted value -- was taken from this table's own emitted artifact by a bench program that includes it, so what was tested is the row that ships and not a copy of it."

    // identity: the accepted values are a SET. A driver that accepts only one of them rejects a genuine part, and a rejected part reads as no part at all -- so on a mismatch, name the id received AND this set.
    public static let IDENTITY_REG: UInt32 = 0x4F
    public static let IDENTITY_WIDTH: UInt32 = 8
    public static let IDENTITY_VALUE_COUNT: UInt32 = 1
    public static let IDENTITY_VALUE_0: UInt32 = 0x40

    // address: a base plus which pin contributes which bit. The part states the range; a carrier fixes the straps, so no strap is defaulted here.
    public static let ADDRESS_BUS: StaticString = "i2c"
    public static let ADDRESS_BASE: UInt32 = 0x1E
    public static let ADDRESS_STRAP_COUNT: UInt32 = 0

    // buses: the register MAP is shared between a part's buses; the ADDRESS TRANSFORM is not, so each states its own as a named dispatch. A path that took the wrong one is wrong by a fixed bit, and on one direction only.
    public static let BUS_COUNT: UInt32 = 2
    public static let BUS_I2C_KIND: StaticString = "i2c"
    public static let BUS_I2C_REGISTER_READ_TRANSFORM: StaticString = "identity"
    public static let BUS_I2C_REGISTER_WRITE_TRANSFORM: StaticString = "identity"
    public static let BUS_I2C_READ_PROTOCOL: StaticString = "write-register-then-repeated-start-read"
    public static let BUS_SPI_KIND: StaticString = "spi"
    public static let BUS_SPI_REGISTER_READ_TRANSFORM: StaticString = "set-bit7"
    public static let BUS_SPI_REGISTER_WRITE_TRANSFORM: StaticString = "clear-bit7"
    public static let BUS_SPI_READ_PROTOCOL: StaticString = "single-chip-select-address-then-clock-data"

    // registers: `_REG` is the address written on the wire -- an operand, NOT an offset to add to an instance base. Fields carry the shifted mask and its shift.
    public static let WHO_AM_I_M_REG: UInt32 = 0x4F
    public static let WHO_AM_I_M_WIDTH: UInt32 = 8
    public static let WHO_AM_I_M_ACCESS: StaticString = "read-only"
    public static let WHO_AM_I_M_WHO_AM_I: UInt32 = 0xFF
    public static let WHO_AM_I_M_WHO_AM_I_LSB: UInt32 = 0
    public static let OFFSET_X_M_REG: UInt32 = 0x45
    public static let OFFSET_X_M_WIDTH: UInt32 = 16
    public static let OFFSET_X_M_ACCESS: StaticString = "read-write"
    public static let OFFSET_X_M_OFFSET_X_L: UInt32 = 0xFF
    public static let OFFSET_X_M_OFFSET_X_L_LSB: UInt32 = 0
    public static let OFFSET_X_M_OFFSET_X_H: UInt32 = 0xFF00
    public static let OFFSET_X_M_OFFSET_X_H_LSB: UInt32 = 8
    public static let OFFSET_Y_M_REG: UInt32 = 0x47
    public static let OFFSET_Y_M_WIDTH: UInt32 = 16
    public static let OFFSET_Y_M_ACCESS: StaticString = "read-write"
    public static let OFFSET_Y_M_OFFSET_Y_L: UInt32 = 0xFF
    public static let OFFSET_Y_M_OFFSET_Y_L_LSB: UInt32 = 0
    public static let OFFSET_Y_M_OFFSET_Y_H: UInt32 = 0xFF00
    public static let OFFSET_Y_M_OFFSET_Y_H_LSB: UInt32 = 8
    public static let OFFSET_Z_M_REG: UInt32 = 0x49
    public static let OFFSET_Z_M_WIDTH: UInt32 = 16
    public static let OFFSET_Z_M_ACCESS: StaticString = "read-write"
    public static let OFFSET_Z_M_OFFSET_Z_L: UInt32 = 0xFF
    public static let OFFSET_Z_M_OFFSET_Z_L_LSB: UInt32 = 0
    public static let OFFSET_Z_M_OFFSET_Z_H: UInt32 = 0xFF00
    public static let OFFSET_Z_M_OFFSET_Z_H_LSB: UInt32 = 8
    public static let CFG_REG_A_M_REG: UInt32 = 0x60
    public static let CFG_REG_A_M_WIDTH: UInt32 = 8
    public static let CFG_REG_A_M_ACCESS: StaticString = "read-write"
    public static let CFG_REG_B_M_REG: UInt32 = 0x61
    public static let CFG_REG_B_M_WIDTH: UInt32 = 8
    public static let CFG_REG_B_M_ACCESS: StaticString = "read-write"
    public static let CFG_REG_C_M_REG: UInt32 = 0x62
    public static let CFG_REG_C_M_WIDTH: UInt32 = 8
    public static let CFG_REG_C_M_ACCESS: StaticString = "read-write"
    public static let INT_CTRL_REG_M_REG: UInt32 = 0x63
    public static let INT_CTRL_REG_M_WIDTH: UInt32 = 8
    public static let INT_CTRL_REG_M_ACCESS: StaticString = "read-write"
    public static let INT_SOURCE_REG_M_REG: UInt32 = 0x64
    public static let INT_SOURCE_REG_M_WIDTH: UInt32 = 8
    public static let INT_SOURCE_REG_M_ACCESS: StaticString = "read-only"
    public static let INT_THS_M_REG: UInt32 = 0x65
    public static let INT_THS_M_WIDTH: UInt32 = 16
    public static let INT_THS_M_ACCESS: StaticString = "read-write"
    public static let INT_THS_M_INT_THS_L: UInt32 = 0xFF
    public static let INT_THS_M_INT_THS_L_LSB: UInt32 = 0
    public static let INT_THS_M_INT_THS_H: UInt32 = 0xFF00
    public static let INT_THS_M_INT_THS_H_LSB: UInt32 = 8
    public static let STATUS_REG_M_REG: UInt32 = 0x67
    public static let STATUS_REG_M_WIDTH: UInt32 = 8
    public static let STATUS_REG_M_ACCESS: StaticString = "read-only"
    public static let OUTX_M_REG: UInt32 = 0x68
    public static let OUTX_M_WIDTH: UInt32 = 16
    public static let OUTX_M_ACCESS: StaticString = "read-only"
    public static let OUTX_M_OUTX_L: UInt32 = 0xFF
    public static let OUTX_M_OUTX_L_LSB: UInt32 = 0
    public static let OUTX_M_OUTX_H: UInt32 = 0xFF00
    public static let OUTX_M_OUTX_H_LSB: UInt32 = 8
    public static let OUTY_M_REG: UInt32 = 0x6A
    public static let OUTY_M_WIDTH: UInt32 = 16
    public static let OUTY_M_ACCESS: StaticString = "read-only"
    public static let OUTY_M_OUTY_L: UInt32 = 0xFF
    public static let OUTY_M_OUTY_L_LSB: UInt32 = 0
    public static let OUTY_M_OUTY_H: UInt32 = 0xFF00
    public static let OUTY_M_OUTY_H_LSB: UInt32 = 8
    public static let OUTZ_M_REG: UInt32 = 0x6C
    public static let OUTZ_M_WIDTH: UInt32 = 16
    public static let OUTZ_M_ACCESS: StaticString = "read-only"
    public static let OUTZ_M_OUTZ_L: UInt32 = 0xFF
    public static let OUTZ_M_OUTZ_L_LSB: UInt32 = 0
    public static let OUTZ_M_OUTZ_H: UInt32 = 0xFF00
    public static let OUTZ_M_OUTZ_H_LSB: UInt32 = 8

    // measurement: the part holds a whole data block steady for the duration of one burst, so a byte-at-a-time read can mix two conversions.
    public static let BURST_LENGTH: UInt32 = 6
}
