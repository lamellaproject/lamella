// GENERATED by lamella-bsp-gen from parts/lsm303agr/lsm303agr-xl.toml + parts/lsm303agr/lsm303agr.toml -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-parts . lsm303agr
//
// The lsm303agr-xl part table, FLATTENED: every value it inherited is spelled out here, because a
// value that is only ever inherited is pinned in no emitted artifact and so cannot be
// checked for drift between languages. A part is something we talk TO rather than a chip
// we run on -- a board states which buses exist, this table states the part's identity,
// and a probe joins the two at run time.

public enum Lsm303agrXlPart {
    public static let PART: StaticString = "lsm303agr-xl"
    public static let FAMILY: StaticString = "lsm303agr"

    // sourcing: how far this part is established, on two INDEPENDENT axes -- a part can be strong on one and absent on the other, which is why they are not one rank. FACTS is `primary` (read from the part's own datasheet) or `secondary` (a primary vendor statement about this part carried by another document, named by DERIVED_FROM). VALIDATION is `none` (no physical part of this type has been made to answer), `identified` (one answered its identity register, against a negative control that tells the part from the wire) or `exercised` (one produced measurements a driver decoded). Anything above `none` states what was observed in EVIDENCE.
    public static let SOURCING_FACTS: StaticString = "primary"
    public static let SOURCING_VALIDATION: StaticString = "identified"
    public static let SOURCING_EVIDENCE: StaticString = "A physical part answered the identity register on a Raspberry Pi Pico 2, over I2C0 at 100 kHz, on a breakout reached through a four-wire cable: register 0x0F read 0x33 at address 0x19, the single value this table accepts at the single address it states, repeated across five consecutive passes with 0 faulted of 112 addresses walked in each. THE NEGATIVE CONTROL RAN AT THE SAME ADDRESS ON THE SAME REGISTER: with the module unplugged, the identical read ABORTED in four consecutive passes, during which nothing answered anywhere on the bus and 0 of 112 addresses were faulted -- so the byte came from a part rather than from the state of an idle wire. A SECOND CONTROL CAME FREE AND IT TESTS THIS FAMILY'S WHOLE MODEL: the other half of the same package was checked in the same passes at ITS address and ITS register, 0x4F at 0x1E, and answered its own value and not this one. Two addresses, two registers, two values, one package -- so the claim that this package is two bus residents is measured rather than argued. THE READING ALSO SETTLED WHICH VARIANT THE BREAKOUT CARRIES: an earlier walk of this same module recorded only that something answered at 0x19 and 0x1E, which narrowed it to an LSM303-class part and no further. EVERY NUMBER IN THE CHECK -- the address, the register and the accepted value -- was taken from this table's own emitted artifact by a bench program that includes it, so what was tested is the row that ships and not a copy of it."

    // identity: the accepted values are a SET. A driver that accepts only one of them rejects a genuine part, and a rejected part reads as no part at all -- so on a mismatch, name the id received AND this set.
    public static let IDENTITY_REG: UInt32 = 0xF
    public static let IDENTITY_WIDTH: UInt32 = 8
    public static let IDENTITY_VALUE_COUNT: UInt32 = 1
    public static let IDENTITY_VALUE_0: UInt32 = 0x33

    // address: a base plus which pin contributes which bit. The part states the range; a carrier fixes the straps, so no strap is defaulted here.
    public static let ADDRESS_BUS: StaticString = "i2c"
    public static let ADDRESS_BASE: UInt32 = 0x19
    public static let ADDRESS_STRAP_COUNT: UInt32 = 0

    // buses: the register MAP is shared between a part's buses; the ADDRESS TRANSFORM is not, so each states its own as a named dispatch. A path that took the wrong one is wrong by a fixed bit, and on one direction only.
    public static let BUS_COUNT: UInt32 = 2
    public static let BUS_I2C_KIND: StaticString = "i2c"
    public static let BUS_I2C_REGISTER_READ_TRANSFORM: StaticString = "identity"
    public static let BUS_I2C_REGISTER_WRITE_TRANSFORM: StaticString = "identity"
    public static let BUS_I2C_READ_PROTOCOL: StaticString = "write-register-then-repeated-start-read"
    public static let BUS_SPI_KIND: StaticString = "spi"
    public static let BUS_SPI_REGISTER_READ_TRANSFORM: StaticString = "set-bit7"
    public static let BUS_SPI_REGISTER_WRITE_TRANSFORM: StaticString = "clear-bit7"
    public static let BUS_SPI_READ_PROTOCOL: StaticString = "single-chip-select-address-then-clock-data"

    // registers: `_REG` is the address written on the wire -- an operand, NOT an offset to add to an instance base. Fields carry the shifted mask and its shift.
    public static let WHO_AM_I_A_REG: UInt32 = 0xF
    public static let WHO_AM_I_A_WIDTH: UInt32 = 8
    public static let WHO_AM_I_A_ACCESS: StaticString = "read-only"
    public static let WHO_AM_I_A_WHO_AM_I: UInt32 = 0xFF
    public static let WHO_AM_I_A_WHO_AM_I_LSB: UInt32 = 0
    public static let STATUS_REG_AUX_A_REG: UInt32 = 0x7
    public static let STATUS_REG_AUX_A_WIDTH: UInt32 = 8
    public static let STATUS_REG_AUX_A_ACCESS: StaticString = "read-only"
    public static let OUT_TEMP_A_REG: UInt32 = 0xC
    public static let OUT_TEMP_A_WIDTH: UInt32 = 16
    public static let OUT_TEMP_A_ACCESS: StaticString = "read-only"
    public static let OUT_TEMP_A_OUT_TEMP_L: UInt32 = 0xFF
    public static let OUT_TEMP_A_OUT_TEMP_L_LSB: UInt32 = 0
    public static let OUT_TEMP_A_OUT_TEMP_H: UInt32 = 0xFF00
    public static let OUT_TEMP_A_OUT_TEMP_H_LSB: UInt32 = 8
    public static let TEMP_CFG_REG_A_REG: UInt32 = 0x1F
    public static let TEMP_CFG_REG_A_WIDTH: UInt32 = 8
    public static let TEMP_CFG_REG_A_ACCESS: StaticString = "read-write"
    public static let CTRL_REG1_A_REG: UInt32 = 0x20
    public static let CTRL_REG1_A_WIDTH: UInt32 = 8
    public static let CTRL_REG1_A_ACCESS: StaticString = "read-write"
    public static let CTRL_REG2_A_REG: UInt32 = 0x21
    public static let CTRL_REG2_A_WIDTH: UInt32 = 8
    public static let CTRL_REG2_A_ACCESS: StaticString = "read-write"
    public static let CTRL_REG3_A_REG: UInt32 = 0x22
    public static let CTRL_REG3_A_WIDTH: UInt32 = 8
    public static let CTRL_REG3_A_ACCESS: StaticString = "read-write"
    public static let CTRL_REG4_A_REG: UInt32 = 0x23
    public static let CTRL_REG4_A_WIDTH: UInt32 = 8
    public static let CTRL_REG4_A_ACCESS: StaticString = "read-write"
    public static let CTRL_REG5_A_REG: UInt32 = 0x24
    public static let CTRL_REG5_A_WIDTH: UInt32 = 8
    public static let CTRL_REG5_A_ACCESS: StaticString = "read-write"
    public static let CTRL_REG6_A_REG: UInt32 = 0x25
    public static let CTRL_REG6_A_WIDTH: UInt32 = 8
    public static let CTRL_REG6_A_ACCESS: StaticString = "read-write"
    public static let STATUS_REG_A_REG: UInt32 = 0x27
    public static let STATUS_REG_A_WIDTH: UInt32 = 8
    public static let STATUS_REG_A_ACCESS: StaticString = "read-only"
    public static let OUT_X_A_REG: UInt32 = 0x28
    public static let OUT_X_A_WIDTH: UInt32 = 16
    public static let OUT_X_A_ACCESS: StaticString = "read-only"
    public static let OUT_X_A_OUT_X_L: UInt32 = 0xFF
    public static let OUT_X_A_OUT_X_L_LSB: UInt32 = 0
    public static let OUT_X_A_OUT_X_H: UInt32 = 0xFF00
    public static let OUT_X_A_OUT_X_H_LSB: UInt32 = 8
    public static let OUT_Y_A_REG: UInt32 = 0x2A
    public static let OUT_Y_A_WIDTH: UInt32 = 16
    public static let OUT_Y_A_ACCESS: StaticString = "read-only"
    public static let OUT_Y_A_OUT_Y_L: UInt32 = 0xFF
    public static let OUT_Y_A_OUT_Y_L_LSB: UInt32 = 0
    public static let OUT_Y_A_OUT_Y_H: UInt32 = 0xFF00
    public static let OUT_Y_A_OUT_Y_H_LSB: UInt32 = 8
    public static let OUT_Z_A_REG: UInt32 = 0x2C
    public static let OUT_Z_A_WIDTH: UInt32 = 16
    public static let OUT_Z_A_ACCESS: StaticString = "read-only"
    public static let OUT_Z_A_OUT_Z_L: UInt32 = 0xFF
    public static let OUT_Z_A_OUT_Z_L_LSB: UInt32 = 0
    public static let OUT_Z_A_OUT_Z_H: UInt32 = 0xFF00
    public static let OUT_Z_A_OUT_Z_H_LSB: UInt32 = 8
    public static let FIFO_CTRL_REG_A_REG: UInt32 = 0x2E
    public static let FIFO_CTRL_REG_A_WIDTH: UInt32 = 8
    public static let FIFO_CTRL_REG_A_ACCESS: StaticString = "read-write"
    public static let FIFO_SRC_REG_A_REG: UInt32 = 0x2F
    public static let FIFO_SRC_REG_A_WIDTH: UInt32 = 8
    public static let FIFO_SRC_REG_A_ACCESS: StaticString = "read-only"

    // measurement: the part holds a whole data block steady for the duration of one burst, so a byte-at-a-time read can mix two conversions.
    public static let BURST_LENGTH: UInt32 = 6
}
