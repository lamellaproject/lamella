# GENERATED by lamella-bsp-gen from parts/l3g/i3g4250d.toml + parts/l3g/l3gd20.toml + parts/l3g/l3g.toml -- DO NOT EDIT.
# Regenerate: cargo run -p lamella-bsp-gen -- gen-parts . l3g
#
# The facts every l3g member emits IDENTICALLY -- the family's INVARIANT subset,
# computed across i3g4250d, l3gd20. A part family's driver base is shared code in every language, and
# this is what it may rely on. A fact is here only while EVERY member agrees on it, so the
# day one member states its own value the fact LEAVES this class and the shared code that
# used it stops compiling. Nothing here is inherited: each member's own table spells all
# of it too, flattened.
FAMILY = "l3g"

# identity: the accepted values are a SET. A driver that accepts only one of them rejects a genuine part, and a rejected part reads as no part at all -- so on a mismatch, name the id received AND this set.
IDENTITY_REG = 0xF
IDENTITY_WIDTH = 8
IDENTITY_VALUE_COUNT = 1

# address: a base plus which pin contributes which bit. The part states the range; a carrier fixes the straps, so no strap is defaulted here.
ADDRESS_BUS = "i2c"
ADDRESS_STRAP_COUNT = 1
ADDRESS_STRAP_SDO_BIT = 0

# buses: the register MAP is shared between a part's buses; the ADDRESS TRANSFORM is not, so each states its own as a named dispatch. A path that took the wrong one is wrong by a fixed bit, and on one direction only.
BUS_COUNT = 2
BUS_I2C_KIND = "i2c"
BUS_I2C_REGISTER_READ_TRANSFORM = "identity"
BUS_I2C_REGISTER_WRITE_TRANSFORM = "identity"
BUS_I2C_READ_PROTOCOL = "write-register-then-repeated-start-read"
BUS_SPI_KIND = "spi"
BUS_SPI_REGISTER_READ_TRANSFORM = "set-bit7"
BUS_SPI_REGISTER_WRITE_TRANSFORM = "clear-bit7"
BUS_SPI_READ_PROTOCOL = "single-chip-select-address-then-clock-data"
BUS_SPI_MODE_COUNT = 1
BUS_SPI_MODE_0 = 3

# registers: `_REG` is the address written on the wire -- an operand, NOT an offset to add to an instance base. Fields carry the shifted mask and its shift.
WHO_AM_I_REG = 0xF
WHO_AM_I_WIDTH = 8
WHO_AM_I_ACCESS = "read-only"
WHO_AM_I_CHIP_ID = 0xFF
WHO_AM_I_CHIP_ID_LSB = 0
CTRL_REG1_REG = 0x20
CTRL_REG1_WIDTH = 8
CTRL_REG1_ACCESS = "read-write"
CTRL_REG1_XEN = 0x1
CTRL_REG1_XEN_LSB = 0
CTRL_REG1_YEN = 0x2
CTRL_REG1_YEN_LSB = 1
CTRL_REG1_ZEN = 0x4
CTRL_REG1_ZEN_LSB = 2
CTRL_REG1_PD = 0x8
CTRL_REG1_PD_LSB = 3
CTRL_REG1_BW = 0x30
CTRL_REG1_BW_LSB = 4
CTRL_REG1_DR = 0xC0
CTRL_REG1_DR_LSB = 6
CTRL_REG2_REG = 0x21
CTRL_REG2_WIDTH = 8
CTRL_REG2_ACCESS = "read-write"
CTRL_REG2_HPCF = 0xF
CTRL_REG2_HPCF_LSB = 0
CTRL_REG2_HPM = 0x30
CTRL_REG2_HPM_LSB = 4
CTRL_REG3_REG = 0x22
CTRL_REG3_WIDTH = 8
CTRL_REG3_ACCESS = "read-write"
CTRL_REG3_I2_EMPTY = 0x1
CTRL_REG3_I2_EMPTY_LSB = 0
CTRL_REG3_I2_ORUN = 0x2
CTRL_REG3_I2_ORUN_LSB = 1
CTRL_REG3_I2_WTM = 0x4
CTRL_REG3_I2_WTM_LSB = 2
CTRL_REG3_I2_DRDY = 0x8
CTRL_REG3_I2_DRDY_LSB = 3
CTRL_REG3_PP_OD = 0x10
CTRL_REG3_PP_OD_LSB = 4
CTRL_REG3_H_LACTIVE = 0x20
CTRL_REG3_H_LACTIVE_LSB = 5
CTRL_REG3_I1_BOOT = 0x40
CTRL_REG3_I1_BOOT_LSB = 6
CTRL_REG3_I1_INT1 = 0x80
CTRL_REG3_I1_INT1_LSB = 7
CTRL_REG5_REG = 0x24
CTRL_REG5_WIDTH = 8
CTRL_REG5_ACCESS = "read-write"
CTRL_REG5_OUT_SEL = 0x3
CTRL_REG5_OUT_SEL_LSB = 0
CTRL_REG5_INT1_SEL = 0xC
CTRL_REG5_INT1_SEL_LSB = 2
CTRL_REG5_HPEN = 0x10
CTRL_REG5_HPEN_LSB = 4
CTRL_REG5_FIFO_EN = 0x40
CTRL_REG5_FIFO_EN_LSB = 6
CTRL_REG5_BOOT = 0x80
CTRL_REG5_BOOT_LSB = 7
REFERENCE_REG = 0x25
REFERENCE_WIDTH = 8
REFERENCE_ACCESS = "read-write"
REFERENCE_REF = 0xFF
REFERENCE_REF_LSB = 0
OUT_TEMP_REG = 0x26
OUT_TEMP_WIDTH = 8
OUT_TEMP_ACCESS = "read-only"
OUT_TEMP_TEMP = 0xFF
OUT_TEMP_TEMP_LSB = 0
STATUS_REG_REG = 0x27
STATUS_REG_WIDTH = 8
STATUS_REG_ACCESS = "read-only"
STATUS_REG_XDA = 0x1
STATUS_REG_XDA_LSB = 0
STATUS_REG_YDA = 0x2
STATUS_REG_YDA_LSB = 1
STATUS_REG_ZDA = 0x4
STATUS_REG_ZDA_LSB = 2
STATUS_REG_ZYXDA = 0x8
STATUS_REG_ZYXDA_LSB = 3
STATUS_REG_XOR = 0x10
STATUS_REG_XOR_LSB = 4
STATUS_REG_YOR = 0x20
STATUS_REG_YOR_LSB = 5
STATUS_REG_ZOR = 0x40
STATUS_REG_ZOR_LSB = 6
STATUS_REG_ZYXOR = 0x80
STATUS_REG_ZYXOR_LSB = 7
OUT_X_REG = 0x28
OUT_X_WIDTH = 16
OUT_X_ACCESS = "read-only"
OUT_X_OUT_X_L = 0xFF
OUT_X_OUT_X_L_LSB = 0
OUT_X_OUT_X_H = 0xFF00
OUT_X_OUT_X_H_LSB = 8
OUT_Y_REG = 0x2A
OUT_Y_WIDTH = 16
OUT_Y_ACCESS = "read-only"
OUT_Y_OUT_Y_L = 0xFF
OUT_Y_OUT_Y_L_LSB = 0
OUT_Y_OUT_Y_H = 0xFF00
OUT_Y_OUT_Y_H_LSB = 8
OUT_Z_REG = 0x2C
OUT_Z_WIDTH = 16
OUT_Z_ACCESS = "read-only"
OUT_Z_OUT_Z_L = 0xFF
OUT_Z_OUT_Z_L_LSB = 0
OUT_Z_OUT_Z_H = 0xFF00
OUT_Z_OUT_Z_H_LSB = 8
FIFO_CTRL_REG_REG = 0x2E
FIFO_CTRL_REG_WIDTH = 8
FIFO_CTRL_REG_ACCESS = "read-write"
FIFO_SRC_REG_REG = 0x2F
FIFO_SRC_REG_WIDTH = 8
FIFO_SRC_REG_ACCESS = "read-only"
INT1_CFG_REG = 0x30
INT1_CFG_WIDTH = 8
INT1_CFG_ACCESS = "read-write"
INT1_SRC_REG = 0x31
INT1_SRC_WIDTH = 8
INT1_SRC_ACCESS = "read-only"
INT1_THS_XH_REG = 0x32
INT1_THS_XH_WIDTH = 8
INT1_THS_XH_ACCESS = "read-write"
INT1_THS_XL_REG = 0x33
INT1_THS_XL_WIDTH = 8
INT1_THS_XL_ACCESS = "read-write"
INT1_THS_YH_REG = 0x34
INT1_THS_YH_WIDTH = 8
INT1_THS_YH_ACCESS = "read-write"
INT1_THS_YL_REG = 0x35
INT1_THS_YL_WIDTH = 8
INT1_THS_YL_ACCESS = "read-write"
INT1_THS_ZH_REG = 0x36
INT1_THS_ZH_WIDTH = 8
INT1_THS_ZH_ACCESS = "read-write"
INT1_THS_ZL_REG = 0x37
INT1_THS_ZL_WIDTH = 8
INT1_THS_ZL_ACCESS = "read-write"
INT1_DURATION_REG = 0x38
INT1_DURATION_WIDTH = 8
INT1_DURATION_ACCESS = "read-write"
CTRL_REG4_REG = 0x23
CTRL_REG4_WIDTH = 8
CTRL_REG4_ACCESS = "read-write"
CTRL_REG4_SIM = 0x1
CTRL_REG4_SIM_LSB = 0
CTRL_REG4_FS = 0x30
CTRL_REG4_FS_LSB = 4
CTRL_REG4_BLE = 0x40
CTRL_REG4_BLE_LSB = 6

# encodings: the codes a field takes, as this part reads them.
SPI_FRAME_READ_BIT = 0x80
SPI_FRAME_AUTO_INCREMENT_BIT = 0x40
SPI_FRAME_ADDRESS_MASK = 0x3F
I2C_FRAME_AUTO_INCREMENT_BIT = 0x80
I2C_FRAME_REGISTER_MASK = 0x7F
SENSITIVITY_UDPS_PER_DIGIT_0 = 8750
SENSITIVITY_UDPS_PER_DIGIT_1 = 17500
SENSITIVITY_UDPS_PER_DIGIT_2 = 70000
SENSITIVITY_UDPS_PER_DIGIT_3 = 70000
CTRL_REG1_MODE_POWER_DOWN = 0x0
CTRL_REG1_MODE_SLEEP = 0x8
CTRL_REG1_MODE_NORMAL_ALL_AXES = 0xF
CTRL_REG1_MODE_RESET = 0x7
HPF_MODE_NORMAL_RESET_ON_REFERENCE_READ = 0
HPF_MODE_REFERENCE_SIGNAL = 1
HPF_MODE_NORMAL = 2
HPF_MODE_AUTORESET_ON_INTERRUPT = 3
FULL_SCALE_DPS_1 = 500
FULL_SCALE_DPS_2 = 2000
FULL_SCALE_DPS_3 = 2000

# measurement: the part holds a whole data block steady for the duration of one burst, so a byte-at-a-time read can mix two conversions.
BURST_LENGTH = 6

# sequence 'enter_normal_mode': declarative steps, resolved here so a step carries its own register and mask. The description is transport agnostic -- one step list describes an I2C transaction, an SPI one, and a call through a host import alike.
ENTER_NORMAL_MODE_STEP_COUNT = 1
ENTER_NORMAL_MODE_STEP_0_OP = "write-field"
ENTER_NORMAL_MODE_STEP_0_TARGET = "ctrl_reg1.pd"
ENTER_NORMAL_MODE_STEP_0_REG = 0x20
ENTER_NORMAL_MODE_STEP_0_MASK = 0x8
ENTER_NORMAL_MODE_STEP_0_LSB = 3
ENTER_NORMAL_MODE_STEP_0_VALUE = 1
ENTER_NORMAL_MODE_STEP_0_BOUNDED = 0

# sequence 'read_rate': declarative steps, resolved here so a step carries its own register and mask. The description is transport agnostic -- one step list describes an I2C transaction, an SPI one, and a call through a host import alike.
READ_RATE_STEP_COUNT = 2
READ_RATE_STEP_0_OP = "poll-field-until"
READ_RATE_STEP_0_TARGET = "status_reg.zyxda"
READ_RATE_STEP_0_REG = 0x27
READ_RATE_STEP_0_MASK = 0x8
READ_RATE_STEP_0_LSB = 3
READ_RATE_STEP_0_VALUE = 1
READ_RATE_STEP_0_BOUNDED = 1
READ_RATE_STEP_1_OP = "burst-read"
READ_RATE_STEP_1_TARGET = "out_x"
READ_RATE_STEP_1_REG = 0x28
READ_RATE_STEP_1_LENGTH = 6
READ_RATE_STEP_1_BOUNDED = 0
