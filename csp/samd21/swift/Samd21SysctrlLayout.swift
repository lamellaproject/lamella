// GENERATED by lamella-bsp-gen from csp/samd21/blocks/sysctrl.toml -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-family . samd21
//
// The samd21 sysctrl block layout: offsets are instance-base-relative (`base + *_OFF`);
// instance bases live in Samd21Instances. Widths are access widths.

public enum Samd21SysctrlLayout {
    // -- register offsets (block-relative) + access widths --
    public static let VREF_OFF: UInt32 = 0x40
    public static let VREF_WIDTH: Int32 = 32

    // -- fields: <REG>_<FIELD> = the shifted mask; _LSB = the shift --
    public static let VREF_TSEN: UInt32 = 0x2
    public static let VREF_TSEN_LSB: UInt32 = 1
    public static let VREF_BGOUTEN: UInt32 = 0x4
    public static let VREF_BGOUTEN_LSB: UInt32 = 2
    public static let VREF_CALIB: UInt32 = 0x7FF0000
    public static let VREF_CALIB_LSB: UInt32 = 16
}
