# PIN map for the SAM E54 family: pin x function -> (instance, signal). EMPTY on purpose --
# identity-first. Rows follow the samd21 shape
# ((pin, function letter) -> (sercom, pad)) when the first binding demands them.

[table]
kind = "pins"
family = "same54"
