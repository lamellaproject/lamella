// GENERATED by lamella-bsp-gen from csp/same54/blocks/flash.toml -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-family . same54
//
// The same54 flash BLOCK layout: offsets are instance-base-relative (`base + *_OFF`);
// instance bases live in Same54Instances. Widths are access widths.

public enum Same54FlashLayout {
    // -- register offsets (block-relative) + access widths --

    // -- fields: <REG>_<FIELD> = the shifted mask; _LSB = the shift --

    // -- block constants --
    public static let ERASED_VALUE: UInt32 = 0xFFFFFFFF
    public static let WRITE_UNIT: UInt32 = 16
}
