# GENERATED by lamella-bsp-gen from bsp/nucleo-f091rc/board.toml + csp/stm32f091/ strata -- DO NOT EDIT.
# Regenerate: cargo run -p lamella-bsp-gen -- gen-family . stm32f091

BOARD = "nucleo-f091rc"
BOARD_MODEL = 7

# Role handles: the ONLY peripheral names an app sees. The value of a
# role handle is its role-id string; the runtime resolves role -> facts through FACTS
# below, never through a surface-private enum.
VCP = "vcp"

CARRIER = {
    "kind": "stlink-vcp",
    "role": "vcp",
}

# Per-role descriptor dicts: the same values the C# NucleoF091rcBindings class carries as consts,
# under one mechanical renaming rule -- an UPPER_SNAKE const there is a lowercase key here,
# grouped by the role it belongs to.
FACTS = {
    "vcp": {
        "kind": "uart",
        "instance": "usart2",
        "base": 0x40004400,
        "rcc_en_reg": 0x4002101C,
        "rcc_en_mask": 0x20000,
        "port_rcc_en_reg": 0x40021014,
        "port_rcc_en_mask": 0x20000,
        "moder_reg": 0x48000000,
        "moder_mask": 0xF0,
        "moder_value": 0xA0,
        "afrl_reg": 0x48000020,
        "afrl_mask": 0xFF00,
        "afrl_value": 0x1100,
        "pclk1_hz": 8000000,
        "brr_115200_hsi_8mhz": 0x45,
    },
}

PLANS = {
    "hsi-8mhz": {"default": True, "source": "hsi", "hsi_hz": 8000000, "pclk1_hz": 8000000},
}

# On-board devices + module control lines (the C# <NAME>_PORT_BASE/_PIN/_MASK/
# _ACTIVE_LOW consts, dict-shaped): PORT group base + pin index + mask + polarity.
DEVICES = {
}
