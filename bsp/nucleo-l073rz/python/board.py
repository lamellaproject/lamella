# GENERATED by lamella-bsp-gen from bsp/nucleo-l073rz/board.toml + csp/stm32l073/ strata -- DO NOT EDIT.
# Regenerate: cargo run -p lamella-bsp-gen -- gen-family . stm32l073

BOARD = "nucleo-l073rz"
BOARD_MODEL = 44
BOARD_VENDOR = "St"

# Role handles: the ONLY peripheral names an app sees. The value of a
# role handle is its role-id string; the runtime resolves role -> facts through FACTS
# below, never through a surface-private enum.
BUTTON = "button"
VCP = "vcp"

CARRIER = {
    "kind": "stlink-vcp",
    "role": "vcp",
}

# Per-role descriptor dicts, grouped by the role each belongs to.
# Emitted from this board's facts, like every other language's support for it:
# an UPPER_SNAKE name in the shared facts is a lowercase key here.
#
# "kind" and "driver_family" are read TOGETHER and neither answers alone: kind is what the
# application asked for -- a uart, an spi -- and driver_family is which REGISTER MAP is behind
# it, as <chip family>-<block>. One SERCOM block serves uart, spi and i2c, so the block does not
# name a driver; a uart is a different register map on every family, so the kind does not either.
FACTS = {
    "button": {
        "kind": "interrupt",
        "driver_family": "stm32l073-exti",
        "token": 0,
        "line": 13,
        "line_mask": 0x2000,
        "nvic_position": 7,
        "exti_base": 0x40010400,
        "exti_pr_reg": 0x40010414,
        "exti_imr_reg": 0x40010400,
        "exti_rtsr_reg": 0x40010408,
        "exti_ftsr_reg": 0x4001040C,
        "port_rcc_en_reg": 0x4002102C,
        "port_rcc_en_mask": 0x4,
        "syscfg_rcc_en_reg": 0x40021034,
        "syscfg_rcc_en_mask": 0x1,
        "exticr_reg": 0x40010014,
        "exticr_mask": 0xF0,
        "exticr_value": 0x20,
    },
    "vcp": {
        "kind": "uart",
        "driver_family": "stm32l073-usart",
        "instance": "usart2",
        "base": 0x40004400,
        "rcc_en_reg": 0x40021038,
        "rcc_en_mask": 0x20000,
        "pclk_hz": 2097000,
        "tx_port_rcc_en_reg": 0x4002102C,
        "tx_port_rcc_en_mask": 0x1,
        "tx_moder_reg": 0x50000000,
        "tx_moder_mask": 0x30,
        "tx_moder_value": 0x20,
        "tx_afr_reg": 0x50000020,
        "tx_afr_mask": 0xF00,
        "tx_afr_value": 0x400,
        "rx_port_rcc_en_reg": 0x4002102C,
        "rx_port_rcc_en_mask": 0x1,
        "rx_moder_reg": 0x50000000,
        "rx_moder_mask": 0xC0,
        "rx_moder_value": 0x80,
        "rx_afr_reg": 0x50000020,
        "rx_afr_mask": 0xF000,
        "rx_afr_value": 0x4000,
        "brr_115200_msi_2mhz": 0x12,
    },
}

# The chip's instance map: every block this family places, with its base address and
# the block layout it follows. A role descriptor above states a PERIPHERAL; a bring-up also
# touches blocks that belong to the chip rather than to any one role -- an oscillator, a clock
# controller, a reset controller -- and those are one per chip, so they are stated once here.
# Each row carries whatever this family's record declares -- a base always, and per-instance
# facts like a clock channel or a bus-enable bit where the family states them. Register offsets
# and bit encodings belong to the driver that knows the block, not here.
INSTANCES = {
    "gpioa": {"block": "gpio", "base": 0x50000000, "rcc_en_off": 0x2C, "rcc_en_bit": 0x0},
    "gpiob": {"block": "gpio", "base": 0x50000400, "rcc_en_off": 0x2C, "rcc_en_bit": 0x1},
    "gpioc": {"block": "gpio", "base": 0x50000800, "rcc_en_off": 0x2C, "rcc_en_bit": 0x2},
    "gpiod": {"block": "gpio", "base": 0x50000C00, "rcc_en_off": 0x2C, "rcc_en_bit": 0x3},
    "gpioe": {"block": "gpio", "base": 0x50001000, "rcc_en_off": 0x2C, "rcc_en_bit": 0x4},
    "gpioh": {"block": "gpio", "base": 0x50001C00, "rcc_en_off": 0x2C, "rcc_en_bit": 0x7},
    "usart1": {"block": "usart", "base": 0x40013800, "rcc_en_off": 0x34, "rcc_en_bit": 0xE},
    "usart2": {"block": "usart", "base": 0x40004400, "rcc_en_off": 0x38, "rcc_en_bit": 0x11},
    "spi1": {"block": "spi", "base": 0x40013000, "rcc_en_off": 0x34, "rcc_en_bit": 0xC},
    "exti": {"block": "exti", "base": 0x40010400},
    "syscfg": {"block": "syscfg", "base": 0x40010000, "rcc_en_off": 0x34, "rcc_en_bit": 0x0},
    "rcc": {"block": "rcc", "base": 0x40021000},
}

PLANS = {
    "msi-2mhz": {"default": True, "source": "msi", "msi_hz": 2097000, "pclk_hz": 2097000},
}

# On-board devices + module control lines: PORT group base + pin index + mask + polarity.
# Emitted from this board's facts; each supported language states the same set in its
# own idiom.
DEVICES = {
    "led": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 5, "mask": 0x20, "active_low": False},
}
