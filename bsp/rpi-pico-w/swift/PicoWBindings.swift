// GENERATED by lamella-bsp-gen from bsp/rpi-pico-w/board.toml + csp/rp2040/ strata -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-family . rp2040
//
// The pico-w board bindings as Swift constants, name/value-identical to PicoWBindings.g.cs:
// every value below is a generation-time literal derived from the strata. Board truth
// lives in board.toml, never here.

public enum PicoWBindings {
    // -- identity --
    public static let BOARD_MODEL: UInt16 = 22
    public static let BOARD_VENDOR: String = "RaspberryPi"

    /// driver family: which REGISTER MAP is behind a role, as `<chip family>-<block>`. The
    /// role's `KIND` says what the application asked for -- a uart, an spi -- and two
    /// peripherals of the same kind can share no register at all, so a consumer that selects a
    /// driver at run time needs both: KIND is what was asked for, this is what the silicon is.
    /// Neither alone is enough. One SERCOM block serves uart, spi and i2c, so the block does not
    /// name a driver; and a uart is a different register map on every family, so the kind does
    /// not either. Derived from the bound instance's block, so it cannot be transcribed wrongly.
    public static let UART0_DRIVER_FAMILY: String = "rp2040-uart"

    // -- UART0: a pl011 uart binding descriptor --
    public static let UART0_BASE: UInt32 = 0x40034000
    public static let UART0_RESET_MASK: UInt32 = 0x400120
    public static let UART0_IO_TX_CTRL: UInt32 = 0x40014004
    public static let UART0_IO_RX_CTRL: UInt32 = 0x4001400C
    public static let UART0_FUNCSEL: UInt32 = 2
    public static let UART0_CLK_PERI_HZ: UInt32 = 12000000
    public static let UART0_IBRD_115200_XOSC_12MHZ: UInt32 = 6
    public static let UART0_FBRD_115200_XOSC_12MHZ: UInt32 = 33

    // -- the clock plan (state-and-verify: the plan states the chosen PLL
    // values AND the resulting rates; generation verified hz == xosc * fbdiv /
    // (postdiv1 * postdiv2) and composed each PRIM word from the pll block fields).
    // Every const carries the plan name as a suffix, the default plan included, so a
    // name states which operating point it means -- a board may declare several --
    public static let XOSC_HZ_PLL_125_48: UInt32 = 12000000
    public static let CLK_SYS_HZ_PLL_125_48: UInt32 = 125000000
    public static let CLK_USB_HZ_PLL_125_48: UInt32 = 48000000
    public static let PLL_SYS_FBDIV_PLL_125_48: UInt32 = 125
    public static let PLL_SYS_PRIM_PLL_125_48: UInt32 = 0x62000
    public static let PLL_USB_FBDIV_PLL_125_48: UInt32 = 100
    public static let PLL_USB_PRIM_PLL_125_48: UInt32 = 0x55000

    // -- on-board devices: PORT group base + pin index + mask --
    public static let CYW43439_WL_REG_ON_PORT_BASE: UInt32 = 0xD0000000
    public static let CYW43439_WL_REG_ON_PIN: UInt32 = 23
    public static let CYW43439_WL_REG_ON_MASK: UInt32 = 0x800000
    public static let CYW43439_WL_REG_ON_ACTIVE_LOW: UInt32 = 0
    public static let CYW43439_DATA_PORT_BASE: UInt32 = 0xD0000000
    public static let CYW43439_DATA_PIN: UInt32 = 24
    public static let CYW43439_DATA_MASK: UInt32 = 0x1000000
    public static let CYW43439_DATA_ACTIVE_LOW: UInt32 = 0
    public static let CYW43439_CS_PORT_BASE: UInt32 = 0xD0000000
    public static let CYW43439_CS_PIN: UInt32 = 25
    public static let CYW43439_CS_MASK: UInt32 = 0x2000000
    public static let CYW43439_CS_ACTIVE_LOW: UInt32 = 1
    public static let CYW43439_CLK_PORT_BASE: UInt32 = 0xD0000000
    public static let CYW43439_CLK_PIN: UInt32 = 29
    public static let CYW43439_CLK_MASK: UInt32 = 0x20000000
    public static let CYW43439_CLK_ACTIVE_LOW: UInt32 = 0

    // -- memory regions the board fits: SIZE is what a program may reach, which a device's own
    // size may exceed; a region with a CONTROLLER does not exist until that instance is brought
    // up, and touching it first is a bus fault rather than a wrong value --
    public static let MEMORY_REGION_COUNT: UInt32 = 1
    public static let MEMORY_FLASH_KIND: StaticString = "flash"
    public static let MEMORY_FLASH_SIZE: UInt32 = 0x200000
    public static let MEMORY_FLASH_OPTIONAL: UInt32 = 0
}
