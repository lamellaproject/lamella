// GENERATED by lamella-bsp-gen from bsp/samda1-xpro/board.toml + csp/samda1/ strata -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-family . samda1
//
// The samda1-xpro board bindings as Swift constants, name/value-identical to Samda1XproBindings.g.cs:
// every value below is a generation-time literal derived from the strata. Board truth
// lives in board.toml, never here.

public enum Samda1XproBindings {
    // -- identity --
    public static let BOARD_MODEL: UInt16 = 49
    public static let BOARD_VENDOR: String = "Microchip"
    public static let CARRIER_USB_VID: UInt16 = 0x03EB

    /// driver family: which REGISTER MAP is behind a role, as `<chip family>-<block>`. The
    /// role's `KIND` says what the application asked for -- a uart, an spi -- and two
    /// peripherals of the same kind can share no register at all, so a consumer that selects a
    /// driver at run time needs both: KIND is what was asked for, this is what the silicon is.
    /// Neither alone is enough. One SERCOM block serves uart, spi and i2c, so the block does not
    /// name a driver; and a uart is a different register map on every family, so the kind does
    /// not either. Derived from the bound instance's block, so it cannot be transcribed wrongly.
    public static let VCP_DRIVER_FAMILY: String = "samda1-sercom"

    // -- VCP: a sercom-usart binding descriptor --
    public static let VCP_SERCOM_BASE: UInt32 = 0x42001400
    public static let VCP_IRQ: UInt32 = 12
    public static let VCP_GCLK_CLKCTRL_VALUE: UInt32 = 0x4017
    public static let VCP_APBC_MASK: UInt32 = 0x20
    public static let VCP_PMUX_REG: UInt32 = 0x4100443B
    public static let VCP_PMUX_PAIR: UInt32 = 0x22
    public static let VCP_PMUX_TX_REG: UInt32 = 0x4100443B
    public static let VCP_PMUX_TX_MASK: UInt32 = 0xF
    public static let VCP_PMUX_TX_VALUE: UInt32 = 0x2
    public static let VCP_PMUX_RX_REG: UInt32 = 0x4100443B
    public static let VCP_PMUX_RX_MASK: UInt32 = 0xF0
    public static let VCP_PMUX_RX_VALUE: UInt32 = 0x20
    public static let VCP_PINCFG_TX_REG: UInt32 = 0x41004456
    public static let VCP_PINCFG_RX_REG: UInt32 = 0x41004457
    public static let VCP_TXPO: UInt32 = 0
    public static let VCP_RXPO: UInt32 = 1
    public static let VCP_BAUD_115200_OSC8M_8MHZ: UInt32 = 0xC505

    // -- on-board devices: PORT group base + pin index + mask --
    public static let LED0_PORT_BASE: UInt32 = 0x41004480
    public static let LED0_PIN: UInt32 = 30
    public static let LED0_MASK: UInt32 = 0x40000000
    public static let LED0_ACTIVE_LOW: UInt32 = 1
    public static let BUTTON0_PORT_BASE: UInt32 = 0x41004400
    public static let BUTTON0_PIN: UInt32 = 15
    public static let BUTTON0_MASK: UInt32 = 0x8000
    public static let BUTTON0_ACTIVE_LOW: UInt32 = 1
}
