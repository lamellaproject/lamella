# GENERATED by lamella-bsp-gen from bsp/micro-bit-v2/board.toml + csp/nrf52833/ strata -- DO NOT EDIT.
# Regenerate: cargo run -p lamella-bsp-gen -- gen-family . nrf52833

BOARD = "micro-bit-v2"
BOARD_MODEL = 11
BOARD_VENDOR = "MicroBit"

# Role handles: the ONLY peripheral names an app sees. The value of a
# role handle is its role-id string; the runtime resolves role -> facts through FACTS
# below, never through a surface-private enum.
INTERNAL_I2C = "internal-i2c"

CARRIER = {
    "kind": "daplink-vcp",
}

# Per-role descriptor dicts, grouped by the role each belongs to.
# Emitted from this board's facts, like every other language's support for it:
# an UPPER_SNAKE name in the shared facts is a lowercase key here.
#
# "kind" and "driver_family" are read TOGETHER and neither answers alone: kind is what the
# application asked for -- a uart, an spi -- and driver_family is which REGISTER MAP is behind
# it, as <chip family>-<block>. One SERCOM block serves uart, spi and i2c, so the block does not
# name a driver; a uart is a different register map on every family, so the kind does not either.
FACTS = {
    "internal-i2c": {
        "kind": "i2c",
        "driver_family": "nrf52833-twi",
        "instance": "twi0",
        "twi_base": 0x40003000,
        "psel_scl": 0x8,
        "psel_sda": 0x10,
        "pin_cnf_scl_reg": 0x50000720,
        "pin_cnf_sda_reg": 0x50000740,
    },
}

# The chip's instance map: every block this family places, with its base address and
# the block layout it follows. A role descriptor above states a PERIPHERAL; a bring-up also
# touches blocks that belong to the chip rather than to any one role -- an oscillator, a clock
# controller, a reset controller -- and those are one per chip, so they are stated once here.
# Bases only: register offsets and bit encodings belong to the driver that knows the block.
INSTANCES = {
    "twi0": {"block": "twi", "base": 0x40003000},
    "port0": {"block": "gpio", "base": 0x50000000},
    "port1": {"block": "gpio", "base": 0x50000300},
}

PLANS = {
    "hfclk-default": {"default": True, "source": "hfclk"},
}

# On-board devices + module control lines: PORT group base + pin index + mask + polarity.
# Emitted from this board's facts; each supported language states the same set in its
# own idiom.
DEVICES = {
    "lsm303agr": {"kind": "i2c-device", "role": "internal-i2c", "address": 0x19},
    "lsm303agr-mag": {"kind": "i2c-device", "role": "internal-i2c", "address": 0x1E},
    "interface-mgmt": {"kind": "i2c-device", "role": "internal-i2c", "address": 0x70},
    "interface-storage": {"kind": "i2c-device", "role": "internal-i2c", "address": 0x72},
    "display-row1": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 21, "mask": 0x200000, "active_low": False},
    "display-row2": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 22, "mask": 0x400000, "active_low": False},
    "display-row3": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 15, "mask": 0x8000, "active_low": False},
    "display-row4": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 24, "mask": 0x1000000, "active_low": False},
    "display-row5": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 19, "mask": 0x80000, "active_low": False},
    "display-col1": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 28, "mask": 0x10000000, "active_low": True},
    "display-col2": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 11, "mask": 0x800, "active_low": True},
    "display-col3": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 31, "mask": 0x80000000, "active_low": True},
    "display-col4": {"kind": "gpio-out", "port_base": 0x50000300, "pin": 5, "mask": 0x20, "active_low": True},
    "display-col5": {"kind": "gpio-out", "port_base": 0x50000000, "pin": 30, "mask": 0x40000000, "active_low": True},
}
