// GENERATED by lamella-bsp-gen from bsp/samc21n-xpro/board.toml + csp/samc21/ strata -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-family . samc21
//
// The samc21n-xpro board bindings as Swift constants, name/value-identical to Samc21nXproBindings.g.cs:
// every value below is a generation-time literal derived from the strata. Board truth
// lives in board.toml, never here.

public enum Samc21nXproBindings {
    // -- identity --
    public static let BOARD_MODEL: UInt16 = 54
    public static let BOARD_VENDOR: String = "Microchip"
    public static let CARRIER_USB_VID: UInt16 = 0x03EB

    /// driver family: which REGISTER MAP is behind a role, as `<chip family>-<block>`. The
    /// role's `KIND` says what the application asked for -- a uart, an spi -- and two
    /// peripherals of the same kind can share no register at all, so a consumer that selects a
    /// driver at run time needs both: KIND is what was asked for, this is what the silicon is.
    /// Neither alone is enough. One SERCOM block serves uart, spi and i2c, so the block does not
    /// name a driver; and a uart is a different register map on every family, so the kind does
    /// not either. Derived from the bound instance's block, so it cannot be transcribed wrongly.
    public static let VCP_DRIVER_FAMILY: String = "samc21-sercom"

        // -- VCP: a sercom-usart binding descriptor on the PCHCTRL clock architecture.
        // The peripheral channel is an ADDRESS here rather than a field -- PCHCTRL[m] --
        // and WHICH APB mask register gates the instance is per-instance --
    public static let VCP_SERCOM_BASE: UInt32 = 0x42001400
    public static let VCP_IRQ: UInt32 = 13
    public static let VCP_GCLK_PCHCTRL_REG: UInt32 = 0x40001CDC
    public static let VCP_GCLK_PCHCTRL_VALUE: UInt32 = 0x40
    public static let VCP_APB_MASK_REG: UInt32 = 0x4000081C
    public static let VCP_APB_MASK: UInt32 = 0x20
    public static let VCP_PMUX_REG: UInt32 = 0x410000B5
    public static let VCP_PMUX_PAIR: UInt32 = 0x33
    public static let VCP_PMUX_TX_REG: UInt32 = 0x410000B5
    public static let VCP_PMUX_TX_MASK: UInt32 = 0xF
    public static let VCP_PMUX_TX_VALUE: UInt32 = 0x3
    public static let VCP_PMUX_RX_REG: UInt32 = 0x410000B5
    public static let VCP_PMUX_RX_MASK: UInt32 = 0xF0
    public static let VCP_PMUX_RX_VALUE: UInt32 = 0x30
    public static let VCP_PINCFG_TX_REG: UInt32 = 0x410000CA
    public static let VCP_PINCFG_RX_REG: UInt32 = 0x410000CB
    public static let VCP_TXPO: UInt32 = 1
    public static let VCP_RXPO: UInt32 = 3
    public static let VCP_BAUD_115200_OSC48M_4MHZ: UInt32 = 0x8A0A

    // -- on-board devices: PORT group base + pin index + mask --
    public static let LED0_PORT_BASE: UInt32 = 0x41000100
    public static let LED0_PIN: UInt32 = 5
    public static let LED0_MASK: UInt32 = 0x20
    public static let LED0_ACTIVE_LOW: UInt32 = 1
    public static let BUTTON0_PORT_BASE: UInt32 = 0x41000080
    public static let BUTTON0_PIN: UInt32 = 19
    public static let BUTTON0_MASK: UInt32 = 0x80000
    public static let BUTTON0_ACTIVE_LOW: UInt32 = 1
}
