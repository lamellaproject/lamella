// GENERATED by lamella-bsp-gen from bsp/nucleo-l053r8/board.toml + csp/stm32l053/ strata -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-family . stm32l053
//
// The nucleo-l053r8 board bindings as Swift constants, name/value-identical to NucleoL053r8Bindings.g.cs:
// every value below is a generation-time literal derived from the strata. Board truth
// lives in board.toml, never here.

public enum NucleoL053r8Bindings {
    // -- identity --
    public static let BOARD_MODEL: UInt16 = 41
    public static let BOARD_VENDOR: String = "St"

    /// driver family: which REGISTER MAP is behind a role, as `<chip family>-<block>`. The
    /// role's `KIND` says what the application asked for -- a uart, an spi -- and two
    /// peripherals of the same kind can share no register at all, so a consumer that selects a
    /// driver at run time needs both: KIND is what was asked for, this is what the silicon is.
    /// Neither alone is enough. One SERCOM block serves uart, spi and i2c, so the block does not
    /// name a driver; and a uart is a different register map on every family, so the kind does
    /// not either. Derived from the bound instance's block, so it cannot be transcribed wrongly.
    public static let VCP_DRIVER_FAMILY: String = "stm32l053-usart"
    public static let BUTTON_DRIVER_FAMILY: String = "stm32l053-exti"

    // -- BUTTON: an st-exti binding descriptor. TOKEN is opaque: the runtime carries it from
    // the ISR to the managed side and never decodes it. Clear EXTI_PR before returning. --
    public static let BUTTON_TOKEN: UInt32 = 0
    public static let BUTTON_EXTI_LINE: UInt32 = 13
    public static let BUTTON_EXTI_LINE_MASK: UInt32 = 0x2000
    public static let BUTTON_NVIC_POSITION: UInt32 = 7
    public static let BUTTON_EXTI_BASE: UInt32 = 0x40010400
    public static let BUTTON_EXTI_PR_REG: UInt32 = 0x40010414
    public static let BUTTON_EXTI_IMR_REG: UInt32 = 0x40010400
    public static let BUTTON_EXTI_RTSR_REG: UInt32 = 0x40010408
    public static let BUTTON_EXTI_FTSR_REG: UInt32 = 0x4001040C
    public static let BUTTON_PORT_RCC_EN_REG: UInt32 = 0x4002102C
    public static let BUTTON_PORT_RCC_EN_MASK: UInt32 = 0x4
    public static let BUTTON_SYSCFG_RCC_EN_REG: UInt32 = 0x40021034
    public static let BUTTON_SYSCFG_RCC_EN_MASK: UInt32 = 0x1
    public static let BUTTON_EXTICR_REG: UInt32 = 0x40010014
    public static let BUTTON_EXTICR_MASK: UInt32 = 0xF0
    public static let BUTTON_EXTICR_VALUE: UInt32 = 0x20

    // -- VCP: an st-usart binding descriptor --
    public static let VCP_BASE: UInt32 = 0x40004400
    public static let VCP_RCC_EN_REG: UInt32 = 0x40021038
    public static let VCP_RCC_EN_MASK: UInt32 = 0x20000
    public static let VCP_TX_PORT_RCC_EN_REG: UInt32 = 0x4002102C
    public static let VCP_TX_PORT_RCC_EN_MASK: UInt32 = 0x1
    public static let VCP_TX_MODER_REG: UInt32 = 0x50000000
    public static let VCP_TX_MODER_MASK: UInt32 = 0x30
    public static let VCP_TX_MODER_VALUE: UInt32 = 0x20
    public static let VCP_TX_AFR_REG: UInt32 = 0x50000020
    public static let VCP_TX_AFR_MASK: UInt32 = 0xF00
    public static let VCP_TX_AFR_VALUE: UInt32 = 0x400
    public static let VCP_RX_PORT_RCC_EN_REG: UInt32 = 0x4002102C
    public static let VCP_RX_PORT_RCC_EN_MASK: UInt32 = 0x1
    public static let VCP_RX_MODER_REG: UInt32 = 0x50000000
    public static let VCP_RX_MODER_MASK: UInt32 = 0xC0
    public static let VCP_RX_MODER_VALUE: UInt32 = 0x80
    public static let VCP_RX_AFR_REG: UInt32 = 0x50000020
    public static let VCP_RX_AFR_MASK: UInt32 = 0xF000
    public static let VCP_RX_AFR_VALUE: UInt32 = 0x4000
    public static let VCP_PCLK_HZ: UInt32 = 2097000
    public static let VCP_BRR_115200_MSI_2MHZ: UInt32 = 0x12
}
