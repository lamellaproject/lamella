# GENERATED by lamella-bsp-gen from bsp/stm32f769i-disco/board.toml + csp/stm32f769/ strata -- DO NOT EDIT.
# Regenerate: cargo run -p lamella-bsp-gen -- gen-family . stm32f769

BOARD = "stm32f769i-disco"
BOARD_MODEL = 30
BOARD_VENDOR = "St"

# Role handles: the ONLY peripheral names an app sees. The value of a
# role handle is its role-id string; the runtime resolves role -> facts through FACTS
# below, never through a surface-private enum.
VCP = "vcp"

CARRIER = {
    "kind": "stlink-vcp",
    "role": "vcp",
}

# Per-role descriptor dicts, grouped by the role each belongs to.
# Emitted from this board's facts, like every other language's support for it:
# an UPPER_SNAKE name in the shared facts is a lowercase key here.
#
# "kind" and "driver_family" are read TOGETHER and neither answers alone: kind is what the
# application asked for -- a uart, an spi -- and driver_family is which REGISTER MAP is behind
# it, as <chip family>-<block>. One SERCOM block serves uart, spi and i2c, so the block does not
# name a driver; a uart is a different register map on every family, so the kind does not either.
FACTS = {
    "vcp": {
        "kind": "uart",
        "driver_family": "stm32f769-usart",
        "instance": "usart1",
        "base": 0x40011000,
        "rcc_en_reg": 0x40023844,
        "rcc_en_mask": 0x10,
        "pclk_hz": 16000000,
        "tx_port_rcc_en_reg": 0x40023830,
        "tx_port_rcc_en_mask": 0x1,
        "tx_moder_reg": 0x40020000,
        "tx_moder_mask": 0xC0000,
        "tx_moder_value": 0x80000,
        "tx_afr_reg": 0x40020024,
        "tx_afr_mask": 0xF0,
        "tx_afr_value": 0x70,
        "rx_port_rcc_en_reg": 0x40023830,
        "rx_port_rcc_en_mask": 0x1,
        "rx_moder_reg": 0x40020000,
        "rx_moder_mask": 0x300000,
        "rx_moder_value": 0x200000,
        "rx_afr_reg": 0x40020024,
        "rx_afr_mask": 0xF00,
        "rx_afr_value": 0x700,
        "brr_115200_hsi_16mhz": 0x8B,
    },
}

# The chip's instance map: every block this family places, with its base address and
# the block layout it follows. A role descriptor above states a PERIPHERAL; a bring-up also
# touches blocks that belong to the chip rather than to any one role -- an oscillator, a clock
# controller, a reset controller -- and those are one per chip, so they are stated once here.
# Bases only: register offsets and bit encodings belong to the driver that knows the block.
INSTANCES = {
    "usart1": {"block": "usart", "base": 0x40011000, "rcc_en_off": 0x44, "rcc_en_bit": 0x4},
    "sdmmc2": {"block": "sdmmc", "base": 0x40011C00, "rcc_en_off": 0x44, "rcc_en_bit": 0x7},
    "gpioa": {"block": "gpio", "base": 0x40020000, "rcc_en_off": 0x30, "rcc_en_bit": 0x0},
    "gpioj": {"block": "gpio", "base": 0x40022400, "rcc_en_off": 0x30, "rcc_en_bit": 0x9},
    "rcc": {"block": "rcc", "base": 0x40023800},
}

PLANS = {
    "hsi-16mhz": {"default": True, "source": "hsi", "hsi_hz": 16000000, "pclk_hz": 16000000},
    "pll-216mhz": {"default": False, "source": "pll", "hsi_hz": 16000000, "pll_m": 8, "pll_n": 216, "pll_p": 2, "pll_q": 9, "sysclk_hz": 216000000, "pll48_hz": 48000000, "pclk_hz": 108000000, "pclk1_hz": 54000000},
}

# On-board devices + module control lines: PORT group base + pin index + mask + polarity.
# Emitted from this board's facts; each supported language states the same set in its
# own idiom.
DEVICES = {
    "led0": {"kind": "gpio-out", "port_base": 0x40022400, "pin": 13, "mask": 0x2000, "active_low": True},
    "led1": {"kind": "gpio-out", "port_base": 0x40022400, "pin": 5, "mask": 0x20, "active_low": True},
    "button0": {"kind": "gpio-in", "port_base": 0x40020000, "pin": 0, "mask": 0x1, "active_low": False},
}
