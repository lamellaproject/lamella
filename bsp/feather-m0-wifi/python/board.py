# GENERATED by lamella-bsp-gen from bsp/feather-m0-wifi/board.toml + csp/samd21/ strata -- DO NOT EDIT.
# Regenerate: cargo run -p lamella-bsp-gen -- gen-family . samd21

BOARD = "feather-m0-wifi"
BOARD_MODEL = 17

# Role handles: the ONLY peripheral names an app sees. The value of a
# role handle is its role-id string; the runtime resolves role -> facts through FACTS
# below, never through a surface-private enum.
WINC_SPI = "winc-spi"

CARRIER = {
    "kind": "native-usb",
}

# Per-role descriptor dicts: the same values the C# FeatherM0WifiBindings class carries as consts,
# under one mechanical renaming rule -- an UPPER_SNAKE const there is a lowercase key here,
# grouped by the role it belongs to.
FACTS = {
    "winc-spi": {
        "kind": "spi",
        "instance": "sercom4",
        "sercom_base": 0x42001800,
        "irq": 13,
        "apbc_mask": 0x40,
        "gclk_core_id": 24,
        "pmux_mosi_reg": 0x410044B5,
        "pmux_mosi_shift": 0,
        "pincfg_mosi_reg": 0x410044CA,
        "pmux_sck_reg": 0x410044B5,
        "pmux_sck_shift": 4,
        "pincfg_sck_reg": 0x410044CB,
        "pmux_miso_reg": 0x41004436,
        "pmux_miso_shift": 0,
        "pincfg_miso_reg": 0x4100444C,
        "pmux_func": 3,
        "dopo": 1,
        "dipo": 0,
        "cs_port_base": 0x41004400,
        "cs_pin": 6,
        "cs_mask": 0x40,
    },
}

PLANS = {
    "osc8m-8mhz": {"default": True, "source": "osc8m", "gclk0_hz": 8000000},
}

# On-board devices + module control lines (the C# <NAME>_PORT_BASE/_PIN/_MASK/
# _ACTIVE_LOW consts, dict-shaped): PORT group base + pin index + mask + polarity.
DEVICES = {
    "winc-reset_n": {"kind": "gpio-out", "port_base": 0x41004400, "pin": 8, "mask": 0x100, "active_low": True},
    "winc-chip_en": {"kind": "gpio-out", "port_base": 0x41004400, "pin": 14, "mask": 0x4000, "active_low": False},
    "winc-irqn": {"kind": "gpio-in", "port_base": 0x41004400, "pin": 21, "mask": 0x200000, "active_low": True},
    "led": {"kind": "gpio-out", "port_base": 0x41004400, "pin": 17, "mask": 0x20000, "active_low": False},
}
