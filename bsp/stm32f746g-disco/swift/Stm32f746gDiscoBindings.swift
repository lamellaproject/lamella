// GENERATED by lamella-bsp-gen from bsp/stm32f746g-disco/board.toml + csp/stm32f7/ strata -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-family . stm32f7
//
// The stm32f746g-disco board bindings as Swift constants, name/value-identical to Stm32f746gDiscoBindings.g.cs:
// every value below is a generation-time literal derived from the strata. Board truth
// lives in board.toml, never here.

public enum Stm32f746gDiscoBindings {
    // -- identity --
    public static let BOARD_MODEL: UInt16 = 20
    public static let BOARD_VENDOR: String = "St"

    /// driver family: which REGISTER MAP is behind a role, as `<chip family>-<block>`. The
    /// role's `KIND` says what the application asked for -- a uart, an spi -- and two
    /// peripherals of the same kind can share no register at all, so a consumer that selects a
    /// driver at run time needs both: KIND is what was asked for, this is what the silicon is.
    /// Neither alone is enough. One SERCOM block serves uart, spi and i2c, so the block does not
    /// name a driver; and a uart is a different register map on every family, so the kind does
    /// not either. Derived from the bound instance's block, so it cannot be transcribed wrongly.
    public static let VCP_DRIVER_FAMILY: String = "stm32f7-usart"
    public static let CODEC_I2C_DRIVER_FAMILY: String = "stm32f7-i2c"
    public static let HEADER_I2C_DRIVER_FAMILY: String = "stm32f7-i2c"

    // -- VCP: an st-usart binding descriptor --
    public static let VCP_BASE: UInt32 = 0x40011000
    public static let VCP_RCC_EN_REG: UInt32 = 0x40023844
    public static let VCP_RCC_EN_MASK: UInt32 = 0x10
    public static let VCP_TX_PORT_RCC_EN_REG: UInt32 = 0x40023830
    public static let VCP_TX_PORT_RCC_EN_MASK: UInt32 = 0x1
    public static let VCP_TX_MODER_REG: UInt32 = 0x40020000
    public static let VCP_TX_MODER_MASK: UInt32 = 0xC0000
    public static let VCP_TX_MODER_VALUE: UInt32 = 0x80000
    public static let VCP_TX_AFR_REG: UInt32 = 0x40020024
    public static let VCP_TX_AFR_MASK: UInt32 = 0xF0
    public static let VCP_TX_AFR_VALUE: UInt32 = 0x70
    public static let VCP_RX_PORT_RCC_EN_REG: UInt32 = 0x40023830
    public static let VCP_RX_PORT_RCC_EN_MASK: UInt32 = 0x2
    public static let VCP_RX_MODER_REG: UInt32 = 0x40020400
    public static let VCP_RX_MODER_MASK: UInt32 = 0xC000
    public static let VCP_RX_MODER_VALUE: UInt32 = 0x8000
    public static let VCP_RX_AFR_REG: UInt32 = 0x40020420
    public static let VCP_RX_AFR_MASK: UInt32 = 0xF0000000
    public static let VCP_RX_AFR_VALUE: UInt32 = 0x70000000
    public static let VCP_PCLK_HZ: UInt32 = 16000000
    public static let VCP_BRR_115200_HSI_16MHZ: UInt32 = 0x8B

    // -- CODEC_I2C: an st-i2c binding descriptor. BOTH PINS ARE OPEN DRAIN --
    // a push-pull output cannot be pulled low by the device at the other end, so an
    // acknowledge is fought instead of seen and nothing ever answers, with the mux
    // perfectly correct. The timing words are the manual's own compliant points at
    // this plan's kernel rate, composed here: five counts, not a divisor --
    public static let CODEC_I2C_BASE: UInt32 = 0x40005C00
    public static let CODEC_I2C_RCC_EN_REG: UInt32 = 0x40023840
    public static let CODEC_I2C_RCC_EN_MASK: UInt32 = 0x800000
    public static let CODEC_I2C_SCL_PORT_RCC_EN_REG: UInt32 = 0x40023830
    public static let CODEC_I2C_SCL_PORT_RCC_EN_MASK: UInt32 = 0x80
    public static let CODEC_I2C_SCL_MODER_REG: UInt32 = 0x40021C00
    public static let CODEC_I2C_SCL_MODER_MASK: UInt32 = 0xC000
    public static let CODEC_I2C_SCL_MODER_VALUE: UInt32 = 0x8000
    public static let CODEC_I2C_SCL_AFR_REG: UInt32 = 0x40021C20
    public static let CODEC_I2C_SCL_AFR_MASK: UInt32 = 0xF0000000
    public static let CODEC_I2C_SCL_AFR_VALUE: UInt32 = 0x40000000
    public static let CODEC_I2C_SDA_PORT_RCC_EN_REG: UInt32 = 0x40023830
    public static let CODEC_I2C_SDA_PORT_RCC_EN_MASK: UInt32 = 0x80
    public static let CODEC_I2C_SDA_MODER_REG: UInt32 = 0x40021C00
    public static let CODEC_I2C_SDA_MODER_MASK: UInt32 = 0x30000
    public static let CODEC_I2C_SDA_MODER_VALUE: UInt32 = 0x20000
    public static let CODEC_I2C_SDA_AFR_REG: UInt32 = 0x40021C24
    public static let CODEC_I2C_SDA_AFR_MASK: UInt32 = 0xF
    public static let CODEC_I2C_SDA_AFR_VALUE: UInt32 = 0x4
    public static let CODEC_I2C_OTYPER_REG: UInt32 = 0x40021C04
    public static let CODEC_I2C_OTYPER_SCL_MASK: UInt32 = 0x80
    public static let CODEC_I2C_OTYPER_SDA_MASK: UInt32 = 0x100
    public static let CODEC_I2C_KERNEL_HZ: UInt32 = 16000000
    public static let CODEC_I2C_TIMINGR_100K_HSI_16MHZ: UInt32 = 0x30420F13
    public static let CODEC_I2C_TIMINGR_400K_HSI_16MHZ: UInt32 = 0x10320309

    // -- HEADER_I2C: an st-i2c binding descriptor. BOTH PINS ARE OPEN DRAIN --
    // a push-pull output cannot be pulled low by the device at the other end, so an
    // acknowledge is fought instead of seen and nothing ever answers, with the mux
    // perfectly correct. The timing words are the manual's own compliant points at
    // this plan's kernel rate, composed here: five counts, not a divisor --
    public static let HEADER_I2C_BASE: UInt32 = 0x40005400
    public static let HEADER_I2C_RCC_EN_REG: UInt32 = 0x40023840
    public static let HEADER_I2C_RCC_EN_MASK: UInt32 = 0x200000
    public static let HEADER_I2C_SCL_PORT_RCC_EN_REG: UInt32 = 0x40023830
    public static let HEADER_I2C_SCL_PORT_RCC_EN_MASK: UInt32 = 0x2
    public static let HEADER_I2C_SCL_MODER_REG: UInt32 = 0x40020400
    public static let HEADER_I2C_SCL_MODER_MASK: UInt32 = 0x30000
    public static let HEADER_I2C_SCL_MODER_VALUE: UInt32 = 0x20000
    public static let HEADER_I2C_SCL_AFR_REG: UInt32 = 0x40020424
    public static let HEADER_I2C_SCL_AFR_MASK: UInt32 = 0xF
    public static let HEADER_I2C_SCL_AFR_VALUE: UInt32 = 0x4
    public static let HEADER_I2C_SDA_PORT_RCC_EN_REG: UInt32 = 0x40023830
    public static let HEADER_I2C_SDA_PORT_RCC_EN_MASK: UInt32 = 0x2
    public static let HEADER_I2C_SDA_MODER_REG: UInt32 = 0x40020400
    public static let HEADER_I2C_SDA_MODER_MASK: UInt32 = 0xC0000
    public static let HEADER_I2C_SDA_MODER_VALUE: UInt32 = 0x80000
    public static let HEADER_I2C_SDA_AFR_REG: UInt32 = 0x40020424
    public static let HEADER_I2C_SDA_AFR_MASK: UInt32 = 0xF0
    public static let HEADER_I2C_SDA_AFR_VALUE: UInt32 = 0x40
    public static let HEADER_I2C_OTYPER_REG: UInt32 = 0x40020404
    public static let HEADER_I2C_OTYPER_SCL_MASK: UInt32 = 0x100
    public static let HEADER_I2C_OTYPER_SDA_MASK: UInt32 = 0x200
    public static let HEADER_I2C_KERNEL_HZ: UInt32 = 16000000
    public static let HEADER_I2C_TIMINGR_100K_HSI_16MHZ: UInt32 = 0x30420F13
    public static let HEADER_I2C_TIMINGR_400K_HSI_16MHZ: UInt32 = 0x10320309

    // -- on-board devices: PORT group base + pin index + mask --
    public static let WM8994_ADDRESS: UInt32 = 0x1A

    // -- memory regions the board fits: SIZE is what a program may reach, which a device's own
    // size may exceed; a region with a CONTROLLER does not exist until that instance is brought
    // up, and touching it first is a bus fault rather than a wrong value --
    public static let MEMORY_REGION_COUNT: UInt32 = 2
    public static let MEMORY_QSPI_KIND: StaticString = "flash"
    public static let MEMORY_QSPI_BASE: UInt32 = 0x90000000
    public static let MEMORY_QSPI_SIZE: UInt32 = 0x1000000
    public static let MEMORY_QSPI_CONTROLLER: StaticString = "quadspi"
    public static let MEMORY_QSPI_OPTIONAL: UInt32 = 1
    public static let MEMORY_SDRAM_KIND: StaticString = "ram"
    public static let MEMORY_SDRAM_BASE: UInt32 = 0xC0000000
    public static let MEMORY_SDRAM_SIZE: UInt32 = 0x800000
    public static let MEMORY_SDRAM_DEVICE_SIZE: UInt32 = 0x1000000
    public static let MEMORY_SDRAM_CONTROLLER: StaticString = "fmc"
    public static let MEMORY_SDRAM_OPTIONAL: UInt32 = 1

    // -- the flash fitted to 'qspi': what the controller must be told about the DEVICE, as
    // opposed to how the controller itself is brought up (which is chip truth and lives in the
    // block layout). FSIZE is derived from the size above rather than stated beside it --
    // 2^(FSIZE+1) bytes -- so a size and an exponent cannot drift apart --
    public static let MEMORY_QSPI_DEVICE_ID: UInt32 = 0x20BA18
    public static let MEMORY_QSPI_ADDRESS_BITS: UInt32 = 24
    public static let MEMORY_QSPI_FSIZE: UInt32 = 23
    public static let MEMORY_QSPI_DCR: UInt32 = 0x170100

    // -- read configurations. Each word below is a COMPLETE command EXCEPT two fields, and
    // which two is the point: the functional mode is the driver's choice (indirect or
    // memory-mapped, both in the block layout), and THE DUMMY COUNT IS THE PART'S ANSWER.
    // A dummy phase covers the device's internal latency in TIME, so the cycles that cover it
    // fall with the interface clock -- and a wrong count does not fail, it shifts every byte
    // and returns plausible garbage. Walk the range and keep the first count that reads back a
    // payload you already know; the anchors below say what this board answered, and at what --
    public static let MEMORY_QSPI_READ_CONFIGURATIONS: UInt32 = 4
    public static let MEMORY_QSPI_DUMMY_MIN: UInt32 = 0
    public static let MEMORY_QSPI_DUMMY_MAX: UInt32 = 31
    public static let MEMORY_QSPI_READ_INSTRUCTION: UInt32 = 0x3
    public static let MEMORY_QSPI_READ_CCR_PHASES: UInt32 = 0x1002503
    public static let MEMORY_QSPI_READ_PRESCALER_HSI_16MHZ: UInt32 = 1
    public static let MEMORY_QSPI_READ_CLOCK_HZ_HSI_16MHZ: UInt32 = 8000000
    public static let MEMORY_QSPI_READ_DUMMY_HSI_16MHZ: UInt32 = 0
    public static let MEMORY_QSPI_FAST_READ_INSTRUCTION: UInt32 = 0xB
    public static let MEMORY_QSPI_FAST_READ_CCR_PHASES: UInt32 = 0x100250B
    public static let MEMORY_QSPI_FAST_READ_PRESCALER_HSI_16MHZ: UInt32 = 0
    public static let MEMORY_QSPI_FAST_READ_CLOCK_HZ_HSI_16MHZ: UInt32 = 16000000
    public static let MEMORY_QSPI_FAST_READ_DUMMY_HSI_16MHZ: UInt32 = 6
    public static let MEMORY_QSPI_FAST_READ_DUMMY_DATASHEET: UInt32 = 8
    public static let MEMORY_QSPI_QUAD_OUTPUT_READ_INSTRUCTION: UInt32 = 0x6B
    public static let MEMORY_QSPI_QUAD_OUTPUT_READ_CCR_PHASES: UInt32 = 0x300256B
    public static let MEMORY_QSPI_QUAD_OUTPUT_READ_PRESCALER_HSI_16MHZ: UInt32 = 0
    public static let MEMORY_QSPI_QUAD_OUTPUT_READ_CLOCK_HZ_HSI_16MHZ: UInt32 = 16000000
    public static let MEMORY_QSPI_QUAD_OUTPUT_READ_DUMMY_HSI_16MHZ: UInt32 = 6
    public static let MEMORY_QSPI_QUAD_OUTPUT_READ_DUMMY_DATASHEET: UInt32 = 8
    public static let MEMORY_QSPI_QUAD_IO_READ_INSTRUCTION: UInt32 = 0xEB
    public static let MEMORY_QSPI_QUAD_IO_READ_CCR_PHASES: UInt32 = 0x3002DEB
    public static let MEMORY_QSPI_QUAD_IO_READ_PRESCALER_HSI_16MHZ: UInt32 = 0
    public static let MEMORY_QSPI_QUAD_IO_READ_CLOCK_HZ_HSI_16MHZ: UInt32 = 16000000
    public static let MEMORY_QSPI_QUAD_IO_READ_DUMMY_HSI_16MHZ: UInt32 = 6
    public static let MEMORY_QSPI_QUAD_IO_READ_DUMMY_DATASHEET: UInt32 = 8

    // -- the SDRAM fitted to 'sdram': the shape its controller has to be told. The geometry and
    // the two sizes above are one fact stated twice and are checked against each other --
    // rows x columns x banks x width IS the size, and a column count off by one halves the
    // memory, aliases every address above the first row, and passes a write-then-read --
    public static let MEMORY_SDRAM_BANK: UInt32 = 1
    public static let MEMORY_SDRAM_SDCR1: UInt32 = 0x1954
    public static let MEMORY_SDRAM_SDCMR_CLOCK_ENABLE: UInt32 = 0x11
    public static let MEMORY_SDRAM_SDCMR_PRECHARGE_ALL: UInt32 = 0x12
    public static let MEMORY_SDRAM_SDCMR_AUTO_REFRESH: UInt32 = 0xF3
    public static let MEMORY_SDRAM_SDCMR_LOAD_MODE: UInt32 = 0x44014
    public static let MEMORY_SDRAM_SETTLE_US: UInt32 = 100

    // -- and the numbers that are NOT constants of anything. Each is derived from a formula
    // that is the CHIP's, a specification that is the DEVICE's, and a clock that is this PLAN's
    // -- write any one of the three down and it is wrong at the other two's next value. They
    // carry the plan's name for that reason. The timings round UP, because a timing is a floor:
    // a cycle short is a violation and a cycle long is only throughput --
    public static let MEMORY_SDRAM_SDCLK_HZ_HSI_16MHZ: UInt32 = 8000000
    public static let MEMORY_SDRAM_REFRESH_COUNT_HSI_16MHZ: UInt32 = 105
    public static let MEMORY_SDRAM_SDRTR_HSI_16MHZ: UInt32 = 0xD2
    public static let MEMORY_SDRAM_SDTR1_HSI_16MHZ: UInt32 = 0x1126361
}
