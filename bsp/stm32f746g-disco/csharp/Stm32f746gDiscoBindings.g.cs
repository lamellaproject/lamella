// GENERATED by lamella-bsp-gen from bsp/stm32f746g-disco/board.toml + csp/stm32f7/ strata -- DO NOT EDIT.
namespace Lamella.Generated
{
    public sealed class Stm32f746gDiscoBindings
    {
        private Stm32f746gDiscoBindings() { }

        public const int BOARD_MODEL = 20;
        public const string BOARD_VENDOR = "St";

        /// driver family: which REGISTER MAP is behind a role, as `<chip family>-<block>`. The
        /// role's `KIND` says what the application asked for -- a uart, an spi -- and two
        /// peripherals of the same kind can share no register at all, so a consumer that selects a
        /// driver at run time needs both: KIND is what was asked for, this is what the silicon is.
        /// Neither alone is enough. One SERCOM block serves uart, spi and i2c, so the block does not
        /// name a driver; and a uart is a different register map on every family, so the kind does
        /// not either. Derived from the bound instance's block, so it cannot be transcribed wrongly.
        public const string VCP_DRIVER_FAMILY = "stm32f7-usart";
        public const string CODEC_I2C_DRIVER_FAMILY = "stm32f7-i2c";
        public const string HEADER_I2C_DRIVER_FAMILY = "stm32f7-i2c";

        public const uint VCP_BASE = 0x40011000;
        public const uint VCP_RCC_EN_REG = 0x40023844;
        public const uint VCP_RCC_EN_MASK = 0x10;
        public const uint VCP_TX_PORT_RCC_EN_REG = 0x40023830;
        public const uint VCP_TX_PORT_RCC_EN_MASK = 0x1;
        public const uint VCP_TX_MODER_REG = 0x40020000;
        public const uint VCP_TX_MODER_MASK = 0xC0000;
        public const uint VCP_TX_MODER_VALUE = 0x80000;
        public const uint VCP_TX_AFR_REG = 0x40020024;
        public const uint VCP_TX_AFR_MASK = 0xF0;
        public const uint VCP_TX_AFR_VALUE = 0x70;
        public const uint VCP_RX_PORT_RCC_EN_REG = 0x40023830;
        public const uint VCP_RX_PORT_RCC_EN_MASK = 0x2;
        public const uint VCP_RX_MODER_REG = 0x40020400;
        public const uint VCP_RX_MODER_MASK = 0xC000;
        public const uint VCP_RX_MODER_VALUE = 0x8000;
        public const uint VCP_RX_AFR_REG = 0x40020420;
        public const uint VCP_RX_AFR_MASK = 0xF0000000;
        public const uint VCP_RX_AFR_VALUE = 0x70000000;
        public const uint VCP_PCLK_HZ = 16000000;
        public const uint VCP_BRR_115200_HSI_16MHZ = 0x8B;

        public const uint CODEC_I2C_BASE = 0x40005C00;
        public const uint CODEC_I2C_RCC_EN_REG = 0x40023840;
        public const uint CODEC_I2C_RCC_EN_MASK = 0x800000;
        public const uint CODEC_I2C_SCL_PORT_RCC_EN_REG = 0x40023830;
        public const uint CODEC_I2C_SCL_PORT_RCC_EN_MASK = 0x80;
        public const uint CODEC_I2C_SCL_MODER_REG = 0x40021C00;
        public const uint CODEC_I2C_SCL_MODER_MASK = 0xC000;
        public const uint CODEC_I2C_SCL_MODER_VALUE = 0x8000;
        public const uint CODEC_I2C_SCL_AFR_REG = 0x40021C20;
        public const uint CODEC_I2C_SCL_AFR_MASK = 0xF0000000;
        public const uint CODEC_I2C_SCL_AFR_VALUE = 0x40000000;
        public const uint CODEC_I2C_SDA_PORT_RCC_EN_REG = 0x40023830;
        public const uint CODEC_I2C_SDA_PORT_RCC_EN_MASK = 0x80;
        public const uint CODEC_I2C_SDA_MODER_REG = 0x40021C00;
        public const uint CODEC_I2C_SDA_MODER_MASK = 0x30000;
        public const uint CODEC_I2C_SDA_MODER_VALUE = 0x20000;
        public const uint CODEC_I2C_SDA_AFR_REG = 0x40021C24;
        public const uint CODEC_I2C_SDA_AFR_MASK = 0xF;
        public const uint CODEC_I2C_SDA_AFR_VALUE = 0x4;
        public const uint CODEC_I2C_OTYPER_REG = 0x40021C04;
        public const uint CODEC_I2C_OTYPER_SCL_MASK = 0x80;
        public const uint CODEC_I2C_OTYPER_SDA_MASK = 0x100;
        public const uint CODEC_I2C_KERNEL_HZ = 16000000;
        public const uint CODEC_I2C_TIMINGR_100K_HSI_16MHZ = 0x30420F13;
        public const uint CODEC_I2C_TIMINGR_400K_HSI_16MHZ = 0x10320309;

        public const uint HEADER_I2C_BASE = 0x40005400;
        public const uint HEADER_I2C_RCC_EN_REG = 0x40023840;
        public const uint HEADER_I2C_RCC_EN_MASK = 0x200000;
        public const uint HEADER_I2C_SCL_PORT_RCC_EN_REG = 0x40023830;
        public const uint HEADER_I2C_SCL_PORT_RCC_EN_MASK = 0x2;
        public const uint HEADER_I2C_SCL_MODER_REG = 0x40020400;
        public const uint HEADER_I2C_SCL_MODER_MASK = 0x30000;
        public const uint HEADER_I2C_SCL_MODER_VALUE = 0x20000;
        public const uint HEADER_I2C_SCL_AFR_REG = 0x40020424;
        public const uint HEADER_I2C_SCL_AFR_MASK = 0xF;
        public const uint HEADER_I2C_SCL_AFR_VALUE = 0x4;
        public const uint HEADER_I2C_SDA_PORT_RCC_EN_REG = 0x40023830;
        public const uint HEADER_I2C_SDA_PORT_RCC_EN_MASK = 0x2;
        public const uint HEADER_I2C_SDA_MODER_REG = 0x40020400;
        public const uint HEADER_I2C_SDA_MODER_MASK = 0xC0000;
        public const uint HEADER_I2C_SDA_MODER_VALUE = 0x80000;
        public const uint HEADER_I2C_SDA_AFR_REG = 0x40020424;
        public const uint HEADER_I2C_SDA_AFR_MASK = 0xF0;
        public const uint HEADER_I2C_SDA_AFR_VALUE = 0x40;
        public const uint HEADER_I2C_OTYPER_REG = 0x40020404;
        public const uint HEADER_I2C_OTYPER_SCL_MASK = 0x100;
        public const uint HEADER_I2C_OTYPER_SDA_MASK = 0x200;
        public const uint HEADER_I2C_KERNEL_HZ = 16000000;
        public const uint HEADER_I2C_TIMINGR_100K_HSI_16MHZ = 0x30420F13;
        public const uint HEADER_I2C_TIMINGR_400K_HSI_16MHZ = 0x10320309;

        public const uint WM8994_ADDRESS = 0x1A;

        /// -- memory regions the board fits: SIZE is what a program may reach, which a device's own
        /// size may exceed; a region with a CONTROLLER does not exist until that instance is brought
        /// up, and touching it first is a bus fault rather than a wrong value --
        public const uint MEMORY_REGION_COUNT = 2;
        public const string MEMORY_QSPI_KIND = "flash";
        public const uint MEMORY_QSPI_BASE = 0x90000000;
        public const uint MEMORY_QSPI_SIZE = 0x1000000;
        public const string MEMORY_QSPI_CONTROLLER = "quadspi";
        public const uint MEMORY_QSPI_OPTIONAL = 1;
        public const string MEMORY_SDRAM_KIND = "ram";
        public const uint MEMORY_SDRAM_BASE = 0xC0000000;
        public const uint MEMORY_SDRAM_SIZE = 0x800000;
        public const uint MEMORY_SDRAM_DEVICE_SIZE = 0x1000000;
        public const string MEMORY_SDRAM_CONTROLLER = "fmc";
        public const uint MEMORY_SDRAM_OPTIONAL = 1;

        /// -- the flash fitted to 'qspi': what the controller must be told about the DEVICE, as
        /// opposed to how the controller itself is brought up (which is chip truth and lives in the
        /// block layout). FSIZE is derived from the size above rather than stated beside it --
        /// 2^(FSIZE+1) bytes -- so a size and an exponent cannot drift apart --
        public const uint MEMORY_QSPI_DEVICE_ID = 0x20BA18;
        public const uint MEMORY_QSPI_ADDRESS_BITS = 24;
        public const uint MEMORY_QSPI_FSIZE = 23;
        public const uint MEMORY_QSPI_DCR = 0x170100;

        /// -- read configurations. Each word below is a COMPLETE command EXCEPT two fields, and
        /// which two is the point: the functional mode is the driver's choice (indirect or
        /// memory-mapped, both in the block layout), and THE DUMMY COUNT IS THE PART'S ANSWER.
        /// A dummy phase covers the device's internal latency in TIME, so the cycles that cover it
        /// fall with the interface clock -- and a wrong count does not fail, it shifts every byte
        /// and returns plausible garbage. Walk the range and keep the first count that reads back a
        /// payload you already know; the anchors below say what this board answered, and at what --
        public const uint MEMORY_QSPI_READ_CONFIGURATIONS = 4;
        public const uint MEMORY_QSPI_DUMMY_MIN = 0;
        public const uint MEMORY_QSPI_DUMMY_MAX = 31;
        public const uint MEMORY_QSPI_READ_INSTRUCTION = 0x3;
        public const uint MEMORY_QSPI_READ_CCR_PHASES = 0x1002503;
        public const uint MEMORY_QSPI_READ_PRESCALER_HSI_16MHZ = 1;
        public const uint MEMORY_QSPI_READ_CLOCK_HZ_HSI_16MHZ = 8000000;
        public const uint MEMORY_QSPI_READ_DUMMY_HSI_16MHZ = 0;
        public const uint MEMORY_QSPI_FAST_READ_INSTRUCTION = 0xB;
        public const uint MEMORY_QSPI_FAST_READ_CCR_PHASES = 0x100250B;
        public const uint MEMORY_QSPI_FAST_READ_PRESCALER_HSI_16MHZ = 0;
        public const uint MEMORY_QSPI_FAST_READ_CLOCK_HZ_HSI_16MHZ = 16000000;
        public const uint MEMORY_QSPI_FAST_READ_DUMMY_HSI_16MHZ = 6;
        public const uint MEMORY_QSPI_FAST_READ_DUMMY_DATASHEET = 8;
        public const uint MEMORY_QSPI_QUAD_OUTPUT_READ_INSTRUCTION = 0x6B;
        public const uint MEMORY_QSPI_QUAD_OUTPUT_READ_CCR_PHASES = 0x300256B;
        public const uint MEMORY_QSPI_QUAD_OUTPUT_READ_PRESCALER_HSI_16MHZ = 0;
        public const uint MEMORY_QSPI_QUAD_OUTPUT_READ_CLOCK_HZ_HSI_16MHZ = 16000000;
        public const uint MEMORY_QSPI_QUAD_OUTPUT_READ_DUMMY_HSI_16MHZ = 6;
        public const uint MEMORY_QSPI_QUAD_OUTPUT_READ_DUMMY_DATASHEET = 8;
        public const uint MEMORY_QSPI_QUAD_IO_READ_INSTRUCTION = 0xEB;
        public const uint MEMORY_QSPI_QUAD_IO_READ_CCR_PHASES = 0x3002DEB;
        public const uint MEMORY_QSPI_QUAD_IO_READ_PRESCALER_HSI_16MHZ = 0;
        public const uint MEMORY_QSPI_QUAD_IO_READ_CLOCK_HZ_HSI_16MHZ = 16000000;
        public const uint MEMORY_QSPI_QUAD_IO_READ_DUMMY_HSI_16MHZ = 6;
        public const uint MEMORY_QSPI_QUAD_IO_READ_DUMMY_DATASHEET = 8;

        /// -- the SDRAM fitted to 'sdram': the shape its controller has to be told. The geometry and
        /// the two sizes above are one fact stated twice and are checked against each other --
        /// rows x columns x banks x width IS the size, and a column count off by one halves the
        /// memory, aliases every address above the first row, and passes a write-then-read --
        public const uint MEMORY_SDRAM_BANK = 1;
        public const uint MEMORY_SDRAM_SDCR1 = 0x1954;
        public const uint MEMORY_SDRAM_SDCMR_CLOCK_ENABLE = 0x11;
        public const uint MEMORY_SDRAM_SDCMR_PRECHARGE_ALL = 0x12;
        public const uint MEMORY_SDRAM_SDCMR_AUTO_REFRESH = 0xF3;
        public const uint MEMORY_SDRAM_SDCMR_LOAD_MODE = 0x44014;
        public const uint MEMORY_SDRAM_SETTLE_US = 100;

        /// -- and the numbers that are NOT constants of anything. Each is derived from a formula
        /// that is the CHIP's, a specification that is the DEVICE's, and a clock that is this PLAN's
        /// -- write any one of the three down and it is wrong at the other two's next value. They
        /// carry the plan's name for that reason. The timings round UP, because a timing is a floor:
        /// a cycle short is a violation and a cycle long is only throughput --
        public const uint MEMORY_SDRAM_SDCLK_HZ_HSI_16MHZ = 8000000;
        public const uint MEMORY_SDRAM_REFRESH_COUNT_HSI_16MHZ = 105;
        public const uint MEMORY_SDRAM_SDRTR_HSI_16MHZ = 0xD2;
        public const uint MEMORY_SDRAM_SDTR1_HSI_16MHZ = 0x1126361;
    }
}
