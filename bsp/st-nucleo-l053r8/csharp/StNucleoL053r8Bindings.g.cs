// GENERATED by lamella-bsp-gen from bsp/st-nucleo-l053r8/board.toml + csp/stm32l053/ strata -- DO NOT EDIT.
// Regenerate: cargo run -p lamella-bsp-gen -- gen-family . stm32l053
//
// The st-nucleo-l053r8 board bindings (resolved against the stm32l053 chip strata): every value below is a
// generation-time literal derived from the strata -- role descriptors, module control
// lines, and the carrier identity. The board's facts are stated in board.toml.
namespace Lamella.Generated
{
    public sealed class StNucleoL053r8Bindings
    {
        private StNucleoL053r8Bindings() { }

        // -- identity --
        public const int BOARD_MODEL = 41;
        public const string BOARD_VENDOR = "St";

        /// driver family: which REGISTER MAP is behind a role, as `<chip family>-<block>`. The
        /// role's `KIND` says what the application asked for -- a uart, an spi -- and two
        /// peripherals of the same kind can share no register at all, so a consumer that selects a
        /// driver at run time needs both: KIND is what was asked for, this is what the silicon is.
        /// Neither alone is enough. One SERCOM block serves uart, spi and i2c, so the block does not
        /// name a driver; and a uart is a different register map on every family, so the kind does
        /// not either. Derived from the bound instance's block, so it cannot be transcribed wrongly.
        public const string VCP_DRIVER_FAMILY = "stm32l053-usart";
        public const string BUTTON_DRIVER_FAMILY = "stm32l053-exti";

        // -- BUTTON: an st-exti binding descriptor. TOKEN is opaque: the runtime carries it
        // from the ISR to the managed side and never decodes it. Clear EXTI_PR before returning
        // or the interrupt re-enters. --
        public const uint BUTTON_TOKEN = 0;
        public const uint BUTTON_EXTI_LINE = 13;
        public const uint BUTTON_EXTI_LINE_MASK = 0x2000;
        public const uint BUTTON_NVIC_POSITION = 7;
        public const uint BUTTON_EXTI_BASE = 0x40010400;
        public const uint BUTTON_EXTI_PR_REG = 0x40010414;
        public const uint BUTTON_EXTI_IMR_REG = 0x40010400;
        public const uint BUTTON_EXTI_RTSR_REG = 0x40010408;
        public const uint BUTTON_EXTI_FTSR_REG = 0x4001040C;
        public const uint BUTTON_PORT_RCC_EN_REG = 0x4002102C;
        public const uint BUTTON_PORT_RCC_EN_MASK = 0x4;
        public const uint BUTTON_SYSCFG_RCC_EN_REG = 0x40021034;
        public const uint BUTTON_SYSCFG_RCC_EN_MASK = 0x1;
        public const uint BUTTON_EXTICR_REG = 0x40010014;
        public const uint BUTTON_EXTICR_MASK = 0xF0;
        public const uint BUTTON_EXTICR_VALUE = 0x20;

        // -- VCP: an st-usart binding descriptor --
        public const uint VCP_BASE = 0x40004400;
        public const uint VCP_RCC_EN_REG = 0x40021038;
        public const uint VCP_RCC_EN_MASK = 0x20000;
        public const uint VCP_TX_PORT_RCC_EN_REG = 0x4002102C;
        public const uint VCP_TX_PORT_RCC_EN_MASK = 0x1;
        public const uint VCP_TX_MODER_REG = 0x50000000;
        public const uint VCP_TX_MODER_MASK = 0x30;
        public const uint VCP_TX_MODER_VALUE = 0x20;
        public const uint VCP_TX_AFR_REG = 0x50000020;
        public const uint VCP_TX_AFR_MASK = 0xF00;
        public const uint VCP_TX_AFR_VALUE = 0x400;
        public const uint VCP_RX_PORT_RCC_EN_REG = 0x4002102C;
        public const uint VCP_RX_PORT_RCC_EN_MASK = 0x1;
        public const uint VCP_RX_MODER_REG = 0x50000000;
        public const uint VCP_RX_MODER_MASK = 0xC0;
        public const uint VCP_RX_MODER_VALUE = 0x80;
        public const uint VCP_RX_AFR_REG = 0x50000020;
        public const uint VCP_RX_AFR_MASK = 0xF000;
        public const uint VCP_RX_AFR_VALUE = 0x4000;
        public const uint VCP_PCLK_HZ = 2097000;
        public const uint VCP_BRR_115200_MSI_2MHZ = 0x12;
        public const uint DEVICE_COUNT = 0;
    }
}
