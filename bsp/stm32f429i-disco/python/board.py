# GENERATED by lamella-bsp-gen from bsp/stm32f429i-disco/board.toml + csp/stm32f42x/ strata -- DO NOT EDIT.
# Regenerate: cargo run -p lamella-bsp-gen -- gen-family . stm32f42x

BOARD = "stm32f429i-disco"
BOARD_MODEL = 21
BOARD_VENDOR = "St"

# Role handles: the ONLY peripheral names an app sees. The value of a
# role handle is its role-id string; the runtime resolves role -> facts through FACTS
# below, never through a surface-private enum.
VCP = "vcp"

CARRIER = {
    "kind": "stlink-vcp",
    "role": "vcp",
}

# Per-role descriptor dicts, grouped by the role each belongs to.
# Emitted from this board's facts, like every other language's support for it:
# an UPPER_SNAKE name in the shared facts is a lowercase key here.
FACTS = {
    "vcp": {
        "kind": "uart",
        "instance": "usart1",
        "base": 0x40011000,
        "rcc_en_reg": 0x40023844,
        "rcc_en_mask": 0x10,
        "pclk_hz": 16000000,
        "tx_port_rcc_en_reg": 0x40023830,
        "tx_port_rcc_en_mask": 0x1,
        "tx_moder_reg": 0x40020000,
        "tx_moder_mask": 0xC0000,
        "tx_moder_value": 0x80000,
        "tx_afr_reg": 0x40020024,
        "tx_afr_mask": 0xF0,
        "tx_afr_value": 0x70,
        "rx_port_rcc_en_reg": 0x40023830,
        "rx_port_rcc_en_mask": 0x1,
        "rx_moder_reg": 0x40020000,
        "rx_moder_mask": 0x300000,
        "rx_moder_value": 0x200000,
        "rx_afr_reg": 0x40020024,
        "rx_afr_mask": 0xF00,
        "rx_afr_value": 0x700,
        "brr_115200_hsi_16mhz": 0x8B,
    },
}

PLANS = {
    "hsi-16mhz": {"default": True, "source": "hsi", "hsi_hz": 16000000, "hclk_hz": 16000000, "pclk_hz": 16000000},
}

# On-board devices + module control lines: PORT group base + pin index + mask + polarity.
# Emitted from this board's facts; each supported language states the same set in its
# own idiom.
DEVICES = {
    "led-green": {"kind": "gpio-out", "port_base": 0x40021800, "pin": 13, "mask": 0x2000, "active_low": False},
    "led-red": {"kind": "gpio-out", "port_base": 0x40021800, "pin": 14, "mask": 0x4000, "active_low": False},
    "button": {"kind": "gpio-in", "port_base": 0x40020000, "pin": 0, "mask": 0x1, "active_low": False},
}

# Memory regions the board fits. A region with a "controller" does not exist until
# that instance is brought up; touching it first is a bus fault, not a wrong value.
MEMORY = {
    "sdram": {"kind": "ram", "base": 0xD0000000, "size": 0x800000, "controller": "fmc", "optional": True, "device": {"bank": 2, "sdcr1": 0x1800, "sdcr2": 0x154, "sdcmr_clock_enable": 0x9, "sdcmr_precharge_all": 0xA, "sdcmr_auto_refresh": 0xEB, "sdcmr_load_mode": 0x4400C, "settle_us": 100, "sdclk_hz_hsi_16mhz": 8000000, "refresh_count_hsi_16mhz": 105, "sdrtr_hsi_16mhz": 0xD2, "sdtr1_hsi_16mhz": 0x106000, "sdtr2_hsi_16mhz": 0x1020361}},
}
