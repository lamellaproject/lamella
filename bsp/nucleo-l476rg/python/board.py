# GENERATED by lamella-bsp-gen from bsp/nucleo-l476rg/board.toml + csp/stm32l476/ strata -- DO NOT EDIT.
# Regenerate: cargo run -p lamella-bsp-gen -- gen-family . stm32l476

BOARD = "nucleo-l476rg"
BOARD_MODEL = 8

# Role handles: the ONLY peripheral names an app sees. The value of a
# role handle is its role-id string; the runtime resolves role -> facts through FACTS
# below, never through a surface-private enum.
VCP = "vcp"

CARRIER = {
    "kind": "stlink-vcp",
    "role": "vcp",
}

# Per-role descriptor dicts: the same values the C# NucleoL476rgBindings class carries as consts,
# under one mechanical renaming rule -- an UPPER_SNAKE const there is a lowercase key here,
# grouped by the role it belongs to.
FACTS = {
    "vcp": {
        "kind": "uart",
        "instance": "usart2",
        "base": 0x40004400,
        "rcc_en_reg": 0x40021058,
        "rcc_en_mask": 0x20000,
        "port_rcc_en_reg": 0x4002104C,
        "port_rcc_en_mask": 0x1,
        "moder_reg": 0x48000000,
        "moder_mask": 0xF0,
        "moder_value": 0xA0,
        "afrl_reg": 0x48000020,
        "afrl_mask": 0xFF00,
        "afrl_value": 0x7700,
        "pclk1_hz": 4000000,
        "brr_115200_msi_4mhz": 0x23,
    },
}

PLANS = {
    "msi-4mhz": {"default": True, "source": "msi", "msi_hz": 4000000, "pclk1_hz": 4000000},
}

# On-board devices + module control lines (the C# <NAME>_PORT_BASE/_PIN/_MASK/
# _ACTIVE_LOW consts, dict-shaped): PORT group base + pin index + mask + polarity.
DEVICES = {
}
